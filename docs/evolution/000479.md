# Generation 479

- **Opened:** 2026-09-15 04:56 UTC
- **Parent generation:** 469
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## How it was planned

- **Plan draft** — approved

  > ...
  > anged — the existing snapshot tests assert
  >   only on the `AgentCycleOutcome` behaviour that is preserved; we only *add* a companion record.
  > - Property check: every symbol named above (`AgentCycleTrace`, `AgentCycleOutcome`,
  >   `AgentCognition`, `Agent.id`, `agent_cognition`, `AgentCycleTrace.cognition`, `run_loop`,
  >   `current_turn`, `agent_cycles`, `AgentTurnRecord`, `AgentTurnTrace`, `cycle_agents`,
  >   `explain_not_running`) is confirmed against the source at edit time by reading that file or
  >   grepping for it — no plausible-sounding name is invented.
  > - No source file other than `harness_tools.py` (imports + trace call + trace field) is modified,
  >   and no new module is added; `cycles.py` already exists and its two functions are only called.
  > 
  > ## Smaller work items for a later split
  > 
  > 1. Add `AgentCycleTrace` typing in `cognition.py`.
  > 2. Import `cycles` in `harness_tools.py` and call `cycle_agents()` on turn end.
  > 3. Call `explain_not_running(self)` and attach the reason to `AgentTurnTrace`.
  > 4. Attach the JSON-safe cognition tail in the turn-end path.
  > 5. Run the import + unit + property checks.
  > 
  > RATIONALE: Wire the dead `cycles.py` module into `harness_tools.py`'s `run_loop` so each harness turn produces a companion `AgentCycleTrace` (carrying the full `AgentCognition` deliberation, not just the `AgentCycleOutcome` final-action snapshot) that `cycle_agents()` groups by agent id and persists through the existing session — recovering *why* an agent acted, not just *what* it decided.

  - **Add the `AgentCycleTrace` dataclass to `cognition.py` next to `AgentCycleOutcome` (fields: `agent_id`, `turn`, `cognition`, `outcome`)** (leaf) — smallest new-typing change confined to one load-bearing module
  - **Import `cycles` into `harness_tools.py` and call `cycles.cycle_agents()` on the `end` action to build the per-cycle record from `agent.agent_cycles[-1]` and `agent.agent_cognition`** (leaf) — depends on: 1
  - **Attach `cycles.explain_not_running(self)` to the `AgentTurnTrace` reason and persist the JSON-safe cognition tail through the existing session** — depends on: 1

## Why this change

**What it set out to do.** --- name: cycle-trace-outcome title: Wire the dead `cycles.py` module so a harness run keeps a full per-agent deliberation trace, not just a final action. status: candidate module: src/evomesh/cycles.py depends: [] --- # Goal The harness's `run_loop()` in `harness_tools.py` is the only place in the repo that actually executes an agent's turn end-to-end. For each turn it already has, in order, the ...

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/cognition.py`** — I added the `AgentCycleTrace` dataclass (fields `agent_id`, `turn`, `cognition`, `outcome`) directly into the already-running `src/evomesh/cognition.py`, placed immediately after the existing `CycleOutcome` dataclass, as the smallest possible typing-only change confined to one load-bearing module.

   ```diff
   --- a/src/evomesh/cognition.py
   +++ b/src/evomesh/cognition.py
   @@ -132,4 +132,12 @@
    
    @dataclass
   +class AgentCycleTrace:
   +    agent_id: str = ""
   +    turn: int = 0
   +    cognition: str = ""
   +    outcome: str = ""
   +
   +
   +@dataclass
    class CycleContext:
        """Everything a behavior may touch during one cycle."""
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
