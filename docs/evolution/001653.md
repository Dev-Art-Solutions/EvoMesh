# Generation 1653

- **Opened:** 2026-09-25 19:44 UTC
- **Parent generation:** 1652
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The current file shows `WorkExecutor(Protocol)` after `WorkOutcome`, so `WorkStatus` isn't in that range — that's fine, it's imported elsewhere. The pattern matches the existing `Counter` usage for `items`, so the edit is correct: iterate `self.improvement_backlog.work_items.values()`, take each `work.status.value`, and count them.

I changed `src/evomesh/environment.py` only: in `Environment.status`, the `improvements` mapping now includes a `work_items` counter that tallies the backlog's work items (steps of multi-step improvements) by their status value, giving the same kind of progress breakdown that already exists for top-level improvement items.

*Standing goal: Implement this improvement to EvoMesh: Show improvement work-item progress in `/improvements` and `/status` [step 2] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/environment.py, `Environment.status`: in the `improvements` mapping, add a `work_items` entry: a count of `self.improvement_backlog.work_items` by status value.
Why the item exists (context, not your task): An improvement with several steps is now worked as a DAG of work items (one per step), but neither `ImprovementControl.summary` (what `/improvements` prints) nor `Environment.status` says how far along those work items are -- a human sees `[active]` and nothing about which step it is on or how many are left.
>                 f"{item.title}"
Already landed: 1. src/evomesh/improvements.py `ImprovementControl.summary` -- for an improvement that has work items, append ` work <completed>/<total>` to its line, counting its work items (from `item.work_item_ids`, looked up in `self.backlog.work_items`) and leaving cancelled ones out of the total.
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/environment.py`** — The current file shows `WorkExecutor(Protocol)` after `WorkOutcome`, so `WorkStatus` isn't in that range — that's fine, it's imported elsewhere. The pattern matches the existing `Counter` usage for `items`, so the edit is correct: iterate `self.improvement_backlog.work_items.values()`, take each `work.status.value`, and count them.

I changed `src/evomesh/environment.py` only: in `Environment.status`, the `improvements` mapping now includes a `work_items` counter that tallies the backlog's work items (steps of multi-step improvements) by their status value, giving the same kind of progress breakdown that already exists for top-level improvement items.

   ```diff
   --- a/src/evomesh/environment.py
   +++ b/src/evomesh/environment.py
   @@ -1636,4 +1636,7 @@
                    "total": len(self.improvement_backlog.items),
                    **Counter(item.status.value for item in self.improvement_backlog.items.values()),
   +                "work_items": Counter(
   +                    work.status.value for work in self.improvement_backlog.work_items.values()
   +                ),
                },
                # Open goals nobody has touched for an hour: work that is
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
