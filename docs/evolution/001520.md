# Generation 1520

- **Opened:** 2026-09-25 03:18 UTC
- **Parent generation:** 1518
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Returning the role itself for unrecognized agents gives each caller a meaningful label (the type name) instead of a generic "agent", and since callers pass the agent type as `role`, a new or custom type is displayed distinctly; the existing test in `tests/test_agent_label.py` only asserts a known role and does not pin the unknown-role behavior, so this change does not break it.

*Standing goal: Refill the improvement backlog from src/evomesh/agent_label.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/agent_label.py and write it down as one new item with its steps -- do not fix it.
What the module is for: Render an agent role as a short human label.
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Set the watcher's command timeout from settings instead of the hardcoded 20s default
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit
  - Log the exception detail when a watcher command times out
  - Delete the lock file in SingletonLock.release()
  - Give `ToolDefinition.parameters_schema` the real JSON schema type of each parameter instead of always "string"*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — Returning the role itself for unrecognized agents gives each caller a meaningful label (the type name) instead of a generic "agent", and since callers pass the agent type as `role`, a new or custom type is displayed distinctly; the existing test in `tests/test_agent_label.py` only asserts a known role and does not pin the unknown-role behavior, so this change does not break it.

   ```diff
   +- [ ] Make `agent_label` return the role itself for roles absent from `_AGENT_LABELS` instead of the hard-coded "agent"
   +    `agent_label(role)` maps known roles to labels but returns the string `"agent"` for anything not in `_AGENT_LABELS`; its callers (`agents.py`, `console.py`) pass `role=agent.type`, so any custom, new, or unknown agent type (e.g. `type="researcher") is rendered as the generic "agent" rather than as `researcher`.
   +    > `    return _AGENT_LABELS.get(role, "agent")`
   +    1. [ ] src/evomesh/agent_label.py `agent_label` -- change the fallback of the `.get` on the final line from the literal `"agent"` to the `role` argument itself (e.g. `return _AGENT_LABELS.get(role, role)`).
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
