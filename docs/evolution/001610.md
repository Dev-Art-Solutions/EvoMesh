# Generation 1610

- **Opened:** 2026-09-25 12:22 UTC
- **Parent generation:** 1609
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Wrapped `await self._client.get(url)` in `_download` with `try/except httpx.HTTPError as exc:` that logs a warning and re-raises `TelegramError("downloading the file failed", retry_after=5)`, so timeouts/read errors are converted into retriable `TelegramError`s like every other HTTP call in the file and the sibling `_send_document`, instead of escaping the poll loop raw.

*Standing goal: Implement this improvement to EvoMesh: Make `_download` catch `httpx` errors and raise a `TelegramError` instead of letting them propagate raw [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/telegram.py, `TelegramChannel._download`: wrap the `await self._client.get(url)` in a `try/except (httpx.HTTPError) as exc:` block that logs the failure and re-raises a `TelegramError` with a retry delay, matching `_send_document`'s handling right next to it.
Why the item exists (context, not your task): `_download` awaits the attachment with `self._client.get(url)` but has no `try/except`, so a `ReadTimeout`/`ReadError` escapes the poll loop (matching the `ReadError`/`ReadTimeout` poll failures logged since the file last changed). Every other HTTP call in the file is wrapped and re-raised as a `TelegramError` with a backoff (`except (httpx.HTTPError, ...) -> ... TelegramError(...)`), and the sibling `_send_document` does the same for uploads — but downloads are unhandled.
>         response = await self._client.get(url)
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/telegram.py`** — Wrapped `await self._client.get(url)` in `_download` with `try/except httpx.HTTPError as exc:` that logs a warning and re-raises `TelegramError("downloading the file failed", retry_after=5)`, so timeouts/read errors are converted into retriable `TelegramError`s like every other HTTP call in the file and the sibling `_send_document`, instead of escaping the poll loop raw.

   ```diff
   --- a/src/evomesh/telegram.py
   +++ b/src/evomesh/telegram.py
   @@ -347,5 +347,9 @@
                raise TelegramError("the Telegram client is not open")
            url = f"{FILE_ROOT}/bot{self.settings.token.strip()}/{file_path}"
   -        response = await self._client.get(url)
   +        try:
   +            response = await self._client.get(url)
   +        except httpx.HTTPError as exc:
   +            logger.warning("Could not download the file: %s", exc)
   +            raise TelegramError("downloading the file failed", retry_after=float(exc.response.elapsed.total_seconds()) if getattr(exc, "response", None) is not None else 5)
            if response.status_code >= 400:
                raise TelegramError(f"downloading the file failed with HTTP {response.status_code}")
   ```

2. **Change to `src/evomesh/telegram.py`** — Wrapped `await self._client.get(url)` in `_download` with `try/except httpx.HTTPError as exc:` that logs a warning and re-raises `TelegramError("downloading the file failed", retry_after=5)`, so timeouts/read errors are converted into retriable `TelegramError`s like every other HTTP call in the file and the sibling `_send_document`, instead of escaping the poll loop raw.

   ```diff
   --- a/src/evomesh/telegram.py
   +++ b/src/evomesh/telegram.py
   @@ -351,5 +351,5 @@
            except httpx.HTTPError as exc:
                logger.warning("Could not download the file: %s", exc)
   -            raise TelegramError("downloading the file failed", retry_after=float(exc.response.elapsed.total_seconds()) if getattr(exc, "response", None) is not None else 5)
   +            raise TelegramError("downloading the file failed", retry_after=5)
            if response.status_code >= 400:
                raise TelegramError(f"downloading the file failed with HTTP {response.status_code}")
   ```

3. **Repair to `src/evomesh/telegram.py`** — The B904 lint required chaining the raised `TelegramError` to the caught exception; adding `from exc` links the new exception to the original `httpx.HTTPError`, distinguishing it from errors raised during handling.

   ```diff
   --- a/src/evomesh/telegram.py
   +++ b/src/evomesh/telegram.py
   @@ -351,5 +351,5 @@
            except httpx.HTTPError as exc:
                logger.warning("Could not download the file: %s", exc)
   -            raise TelegramError("downloading the file failed", retry_after=5)
   +            raise TelegramError("downloading the file failed", retry_after=5) from exc
            if response.status_code >= 400:
                raise TelegramError(f"downloading the file failed with HTTP {response.status_code}")
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
