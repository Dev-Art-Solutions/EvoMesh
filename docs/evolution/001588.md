# Generation 1588

- **Opened:** 2026-09-25 09:04 UTC
- **Parent generation:** 1586
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Replaced the unknown-phase fallback in `src/evomesh/phase_label.py` with `return text[:1].upper() + text[1:] or None` (keeping the upper-casing for non-empty inputs) and widened the return annotation from `str` to `str | None`; this makes empty/whitespace/`None` inputs return `None` per the documented contract without altering the existing `phase_label("planning_phase") == "Planning phase"` behavior that the literal `text or None` substitution would have broken.

*Standing goal: Implement this improvement to EvoMesh: Fix `phase_label` to return `None` for an empty/whitespace/`None` phase, matching its documented contract ("empty or `None` -> `None`") [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/phase_label.py, `phase_label`: in the unknown-phase fallback, replace `return text[:1].upper() + text[1:] or "Unknown"` with `return text or None` (so empty/whitespace/`None` yields `None`), keeping the unknown-but-non-empty case returning `"Unknown"`.
Why the item exists (context, not your task): `phase_label` violates its own documented contract (stated in the module docstring at the top and quoted in `tests/test_phase_label.py`): for an empty or whitespace input it returns `"Unknown"` instead of `None`. The unknown-phase fallback `return text[:1].upper() + text[1:] or "Unknown"` evaluates to `"Unknown"` for `""` and `"   "` (an all-spaces input capitalizes to `""`, whose `or` falls through), so `phase_label("")` and `phase_label("   ")` both yield `"Unknown"`. This is verified: `phase_label("")` returns `'Unknown'` and `phase_label("   ")` returns `'Unknown'`, whereas the contract sa [...]
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.

The last attempt at this exact target failed. Read this before trying again, so you do not repeat it:
`uv run pyright` failed:
WARNING: there is a new pyright version available (v1.1.411 -> v1.1.414). Please install the new version or set PYRIGHT_PYTHON_FORCE_VERSION to `latest` d:\Projects\Dev-art solutions\EvoMesh\generations\001587-candidate\src\evomesh\phase_label.py d:\Projects\Dev-art solutions\EvoMesh\generations\001587-candidate\src\evomesh\phase_label.py:48:16 - error: Type "str | None" is not assignable to return type "str" Type "str | None" is not assignable to type "str" "None" is not assignable to "str" (reportReturnType) 1 error, 0 warnings, 0 informations*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/phase_label.py`** — Replaced the unknown-phase fallback in `src/evomesh/phase_label.py` with `return text[:1].upper() + text[1:] or None` (keeping the upper-casing for non-empty inputs) and widened the return annotation from `str` to `str | None`; this makes empty/whitespace/`None` inputs return `None` per the documented contract without altering the existing `phase_label("planning_phase") == "Planning phase"` behavior that the literal `text or None` substitution would have broken.

   ```diff
   --- a/src/evomesh/phase_label.py
   +++ b/src/evomesh/phase_label.py
   @@ -39,6 +39,11 @@
    
    
   -def phase_label(phase: AgentPhase | str) -> str:
   -    """Render any agent phase as a short human label."""
   +def phase_label(phase: AgentPhase | str) -> str | None:
   +    """Render any agent phase as a short human label.
   +
   +    Empty, whitespace-only, or ``None`` input yields ``None``; every other
   +    (unknown) phase renders as the first character upper-cased and the rest
   +    preserved.
   +    """
        text = phase.value if isinstance(phase, AgentPhase) else str(phase).strip()
        try:
   @@ -46,3 +51,3 @@
        except KeyError:
            text = text.replace("_", " ").strip()
   -        return text[:1].upper() + text[1:] or "Unknown"+        return text[:1].upper() + text[1:] or None
   ```

2. **Change to `tests/test_phase_label.py`** — Replaced the unknown-phase fallback in `src/evomesh/phase_label.py` with `return text[:1].upper() + text[1:] or None` (keeping the upper-casing for non-empty inputs) and widened the return annotation from `str` to `str | None`; this makes empty/whitespace/`None` inputs return `None` per the documented contract without altering the existing `phase_label("planning_phase") == "Planning phase"` behavior that the literal `text or None` substitution would have broken.

   ```diff
   --- a/tests/test_phase_label.py
   +++ b/tests/test_phase_label.py
   @@ -21,2 +21,15 @@
    def test_phase_label_capitalizes_only_the_first_char():
        assert phase_label("planning_phase") == "Planning phase"
   +
   +
   +def test_phase_label_none_for_empty_whitespace_and_none():
   +    # Documented contract: empty, whitespace-only, or None input yields None.
   +    assert phase_label("") is None
   +    assert phase_label("   ") is None
   +    assert phase_label(None) is None
   +
   +
   +def test_phase_label_formats_non_empty_unknown_phase():
   +    # Empty/whitespace is the only None case; non-empty unknown phases are
   +    # still upper-cased, so this returns a formatted label, not None.
   +    assert phase_label("unknown_phase") == "Unknown phase"
   ```

3. **Repair to `src/evomesh/phase_label.py`** — Pyright failed because `None` wasn't assignable to `AgentPhase | str`. I widened the parameter type to `AgentPhase | str | None` to match the documented contract, and added a `if phase is None: return None` guard so the runtime actually returns `None` (the prior logic would have computed `str(None)` = `"None"` and returned that instead).

   ```diff
   --- a/src/evomesh/phase_label.py
   +++ b/src/evomesh/phase_label.py
   @@ -39,5 +39,5 @@
    
    
   -def phase_label(phase: AgentPhase | str) -> str | None:
   +def phase_label(phase: AgentPhase | str | None) -> str | None:
        """Render any agent phase as a short human label.
   ```

4. **Repair to `src/evomesh/phase_label.py`** — Pyright failed because `None` wasn't assignable to `AgentPhase | str`. I widened the parameter type to `AgentPhase | str | None` to match the documented contract, and added a `if phase is None: return None` guard so the runtime actually returns `None` (the prior logic would have computed `str(None)` = `"None"` and returned that instead).

   ```diff
   --- a/src/evomesh/phase_label.py
   +++ b/src/evomesh/phase_label.py
   @@ -46,4 +46,6 @@
        preserved.
        """
   +    if phase is None:
   +        return None
        text = phase.value if isinstance(phase, AgentPhase) else str(phase).strip()
        try:
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
