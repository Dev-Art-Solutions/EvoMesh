# Generation 1371

- **Opened:** 2026-09-24 08:40 UTC
- **Parent generation:** 1370
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I changed `src/evomesh/telegram.py` only — logging `type(exc).__name__` alongside the (often empty) message so a failed poll records its cause, and replacing the fixed 5-second sleep with a capped exponential backoff (5s→10s→20s…→120s) that resets to 5s after any successful poll, which directly addresses both problems in the improvement backlog item without touching docs.

*Standing goal: Implement this improvement to EvoMesh: Back off exponentially when Telegram polling keeps failing, and log what actually failed
`TelegramChannel`'s poll loop in src/evomesh/telegram.py catches `httpx.HTTPError` and
logs `"Telegram poll failed, retrying: %s", exc`, then sleeps a fixed 5 seconds. Two real
problems, both visible in mesh.log: an `httpx.ReadTimeout` stringifies to an empty string,
so dozens of lines read just `retrying: ` with no cause; and a network outage retries every
5s forever. Log `type(exc).__name__` alongside the message, and double the delay on each
consecutive failure (5s, 10s, 20s ... capped at 120s), resetting to 5s after one success.
It comes from the project's own improvement backlog (docs/evolution/improvements.md). This is a change to how EvoMesh behaves, so it must change at least one file under src/evomesh/ -- a test alone is not an answer, and a candidate that only touches tests or docs is discarded unvalidated. A test covering the new behavior is welcome alongside the source change.
Do not edit docs/evolution/improvements.md yourself: the pipeline ticks this item off once your change is in.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/telegram.py`** — I changed `src/evomesh/telegram.py` only — logging `type(exc).__name__` alongside the (often empty) message so a failed poll records its cause, and replacing the fixed 5-second sleep with a capped exponential backoff (5s→10s→20s…→120s) that resets to 5s after any successful poll, which directly addresses both problems in the improvement backlog item without touching docs.

   ```diff
   --- a/src/evomesh/telegram.py
   +++ b/src/evomesh/telegram.py
   @@ -79,4 +79,6 @@
            self._offset = 0
            self._running = False
   +        # Exponential backoff for consecutive poll failures, reset after a success.
   +        self._backoff = BACKOFF_INTERVAL_SECONDS
            self.identity = ""
            # None: the mesh-wide bot, talking to whichever agent /chat selected.
   ```

2. **Change to `src/evomesh/telegram.py`** — I changed `src/evomesh/telegram.py` only — logging `type(exc).__name__` alongside the (often empty) message so a failed poll records its cause, and replacing the fixed 5-second sleep with a capped exponential backoff (5s→10s→20s…→120s) that resets to 5s after any successful poll, which directly addresses both problems in the improvement backlog item without touching docs.

   ```diff
   --- a/src/evomesh/telegram.py
   +++ b/src/evomesh/telegram.py
   @@ -238,7 +238,14 @@
                    raise
                except (httpx.HTTPError, TelegramError) as exc:
   -                logger.warning("Telegram poll failed, retrying: %s", exc)
   -                await asyncio.sleep(5)
   +                logger.warning(
   +                    "Telegram poll failed, retrying in %.0fs: %s: %s",
   +                    self._backoff,
   +                    type(exc).__name__,
   +                    exc,
   +                )
   +                await asyncio.sleep(self._backoff)
   +                self._backoff = min(self._backoff * 2, BACKOFF_MAX_SECONDS)
                    continue
   +            self._backoff = BACKOFF_INTERVAL_SECONDS
                for update in updates if isinstance(updates, list) else []:
                    await self._consume(update)
   ```

3. **Change to `src/evomesh/telegram.py`** — I changed `src/evomesh/telegram.py` only — logging `type(exc).__name__` alongside the (often empty) message so a failed poll records its cause, and replacing the fixed 5-second sleep with a capped exponential backoff (5s→10s→20s…→120s) that resets to 5s after any successful poll, which directly addresses both problems in the improvement backlog item without touching docs.

   ```diff
   --- a/src/evomesh/telegram.py
   +++ b/src/evomesh/telegram.py
   @@ -38,4 +38,9 @@
    # a status listing cut in half is worse than one that arrives in two messages.
    MESSAGE_LIMIT = 3800
   +
   +# Base backoff interval and its ceiling for consecutive poll failures, which
   +# double each time in a row (5s, 10s, 20s ...) until one succeeds again.
   +BACKOFF_INTERVAL_SECONDS = 5
   +BACKOFF_MAX_SECONDS = 120
    
    WELCOME = (
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
