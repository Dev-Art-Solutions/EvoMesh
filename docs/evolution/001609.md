# Generation 1609

- **Opened:** 2026-09-25 11:59 UTC
- **Parent generation:** 1595
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I have everything I need. Let me summarize my findings:

- The `_poll` retry-backoff was **already fixed** (the log line about poll ReadTimeout/ReadError predates that fix) — so that's not my item.
- Throughout the file, every HTTP-call site catches `httpx.HTTPError` and converts it into a `TelegramError` with a retry delay (lines 168, 200, 249, 386).
- **`_download` is the sole exception**: it awaits `self._client.get(url)` with no `try/except`, so a `ReadTimeout`/`ReadError` propagates raw up through `_consume` → the poll loop, crashing the whole loop for one failed attachment fetch.
- The analogous `_send_document` right next to it *does* do the right thing.

I'll write the item as a behavior change in `src/evomesh/`: make `_download` catch and re-raise. I anchored the new lines against the existing `_send_document` method it goes next to, and I copied a `>` quote character-for-character.

[ ] Make `_download` catch `httpx` errors and raise a `TelegramError` instead of letting them propagate raw
`_download` awaits the attachment with `self._client.get(url)` but has no `try/except`, so a `ReadTimeout`/`ReadError` escapes the poll loop (matching the `ReadError`/`ReadTimeout` poll failures logged since the file last changed). Every other HTTP call in the file is wrapped and re-raised as a `TelegramError` with a backoff (`except (httpx.HTTPError, ...) -> ... TelegramError(...)`), and the sibling `_send_document` does the same for uploads — but downloads are unhandled.
>         response = await self._client.get(url)
1. src/evomesh/telegram.py `TelegramChannel._download` -- wrap the `await self._client.get(url)` in a `try/except (httpx.HTTPError) as exc:` block that logs the failure and re-raises a `TelegramError` with a retry delay, matching `_send_document`'s handling right next to it.
- RATIONALE: this closes the one uncovered HTTP site so a failed attachment fetch fails gracefully (logged + retried with backoff) instead of crashing the whole poll loop, and it matches the existing error pattern in `_send_document`.

*Standing goal: Refill the improvement backlog from src/evomesh/telegram.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/telegram.py and write it down as one new item with its steps -- do not fix it.
What the module is for: A Telegram bot as a second console onto the same running mesh.
What the running mesh logged from it since the file last changed (numbers masked) -- the strongest lead there is:
  - 2x Telegram poll failed, retrying in Ns: ReadError: 
  - 2x Telegram poll failed, retrying in Ns: ReadTimeout: 
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Set the watcher's command timeout from settings instead of the hardcoded 20s default
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit
  - Log the exception detail when a watcher command times out
  - Delete the lock file in SingletonLock.release()
  - Give `ToolDefinition.parameters_schema` the real JSON schema type of each parameter instead of always "string"
  - Make `agent_label` return the role itself for roles absent from `_AGENT_LABELS` instead of the hard-coded "agent"
  - Capitalize only the first letter when rendering an unknown phase, not the whole string
  - Add backoff so a watcher that keeps timing out slows down instead of hammering the failing command every interval
  - Make `_sum` use a correctly-rounded float sum instead of a running total
  - Fix `phase_label` to return `None` for an empty/whitespace/`None` phase, matching its documented contract ("empty or `None` -> `None`")
  - Validate a tool parameter's declared `type` when parsing the tool, rejecting unrecognized ones

The last attempt at this exact target failed. Read this before trying again, so you do not repeat it:
it made no real edit at all -- the candidate was discarded before anything could even be validated.*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — I have everything I need. Let me summarize my findings:

- The `_poll` retry-backoff was **already fixed** (the log line about poll ReadTimeout/ReadError predates that fix) — so that's not my item.
- Throughout the file, every HTTP-call site catches `httpx.HTTPError` and converts it into a `TelegramError` with a retry delay (lines 168, 200, 249, 386).
- **`_download` is the sole exception**: it awaits `self._client.get(url)` with no `try/except`, so a `ReadTimeout`/`ReadError` propagates raw up through `_consume` → the poll loop, crashing the whole loop for one failed attachment fetch.
- The analogous `_send_document` right next to it *does* do the right thing.

I'll write the item as a behavior change in `src/evomesh/`: make `_download` catch and re-raise. I anchored the new lines against the existing `_send_document` method it goes next to, and I copied a `>` quote character-for-character.

[ ] Make `_download` catch `httpx` errors and raise a `TelegramError` instead of letting them propagate raw
`_download` awaits the attachment with `self._client.get(url)` but has no `try/except`, so a `ReadTimeout`/`ReadError` escapes the poll loop (matching the `ReadError`/`ReadTimeout` poll failures logged since the file last changed). Every other HTTP call in the file is wrapped and re-raised as a `TelegramError` with a backoff (`except (httpx.HTTPError, ...) -> ... TelegramError(...)`), and the sibling `_send_document` does the same for uploads — but downloads are unhandled.
>         response = await self._client.get(url)
1. src/evomesh/telegram.py `TelegramChannel._download` -- wrap the `await self._client.get(url)` in a `try/except (httpx.HTTPError) as exc:` block that logs the failure and re-raises a `TelegramError` with a retry delay, matching `_send_document`'s handling right next to it.
- RATIONALE: this closes the one uncovered HTTP site so a failed attachment fetch fails gracefully (logged + retried with backoff) instead of crashing the whole poll loop, and it matches the existing error pattern in `_send_document`.

   ```diff
   +- [ ] Make `_download` catch `httpx` errors and raise a `TelegramError` instead of letting them propagate raw
   +    `_download` awaits the attachment with `self._client.get(url)` but has no `try/except`, so a `ReadTimeout`/`ReadError` escapes the poll loop (matching the `ReadError`/`ReadTimeout` poll failures logged since the file last changed). Every other HTTP call in the file is wrapped and re-raised as a `TelegramError` with a backoff (`except (httpx.HTTPError, ...) -> ... TelegramError(...)`), and the sibling `_send_document` does the same for uploads — but downloads are unhandled.
   +    >         response = await self._client.get(url)
   +    1. [ ] src/evomesh/telegram.py `TelegramChannel._download` -- wrap the `await self._client.get(url)` in a `try/except (httpx.HTTPError) as exc:` block that logs the failure and re-raises a `TelegramError` with a retry delay, matching `_send_document`'s handling right next to it.
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
