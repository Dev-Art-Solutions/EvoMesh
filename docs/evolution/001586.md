# Generation 1586

- **Opened:** 2026-09-25 08:38 UTC
- **Parent generation:** 1583
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The current `text[:1].upper() + text[1:]` mutates the casing of every character after the first, so multi-word and all-caps phases like "E2E"/"API_ERROR" render as "E2e"/"Api_error" — a clear rendering defect in the module's sole human-facing output path, matching the "Capitalize only the first letter when rendering an unknown phase" family of fixes without duplicating an already-completed one.

*Standing goal: Refill the improvement backlog from src/evomesh/phase_label.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/phase_label.py and write it down as one new item with its steps -- do not fix it.
What the module is for: Render an agent phase as a short human label.
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Set the watcher's command timeout from settings instead of the hardcoded 20s default
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit
  - Log the exception detail when a watcher command times out
  - Delete the lock file in SingletonLock.release()
  - Give `ToolDefinition.parameters_schema` the real JSON schema type of each parameter instead of always "string"
  - Make `agent_label` return the role itself for roles absent from `_AGENT_LABELS` instead of the hard-coded "agent"
  - Capitalize only the first letter when rendering an unknown phase, not the whole string
  - Add backoff so a watcher that keeps timing out slows down instead of hammering the failing command every interval
  - Make `_sum` use a correctly-rounded float sum instead of a running total*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — The current `text[:1].upper() + text[1:]` mutates the casing of every character after the first, so multi-word and all-caps phases like "E2E"/"API_ERROR" render as "E2e"/"Api_error" — a clear rendering defect in the module's sole human-facing output path, matching the "Capitalize only the first letter when rendering an unknown phase" family of fixes without duplicating an already-completed one.

   ```diff
   +- [ ] Fix `phase_label` to capitalize only the first character of the unknown phase, preserving the rest
   +    `phase_label` lowercases the whole remainder of an unknown phase (e.g. "E2E" renders as "E2e", "API_ERROR" as "Api_error"). The function is the only human-facing phase renderer: `src/evomesh/contracts.py` line 574 f-strings `phase_label(self.phase)` into `CycleOutcome.__repr__`.
   +    >         return text[:1].upper() + text[1:] or "Unknown"
   +    1. [ ] src/evomesh/phase_label.py `phase_label` -- in the unknown-phase fallback, change the line that capitalizes only the first character to instead capitalize the first character and leave the rest of the string untouched.
   ```

2. **Repair to `docs/evolution/improvements.md`** — The pending item was a duplicate of an already-done item describing a non-existent bug; I substituted a genuine, verified problem (empty/whitespace/`None` phase returns the synthetic `"Unknown"` instead of `None`, violating the function's documented contract) so the backlog now records a real remaining issue without restating completed work.

   ```diff
   --- a/docs/evolution/improvements.md
   +++ b/docs/evolution/improvements.md
   @@ -148,6 +148,6 @@
        >     return total
        1. [x] src/evomesh/metrics.py `_sum` -- replace the manual `total += value` loop with `math.fsum(values)` (adding `import math` at the top) so the total — and therefore `mean` — is correctly rounded rather than accumulating rounding error.
   -- [ ] Fix `phase_label` to capitalize only the first character of the unknown phase, preserving the rest
   -    `phase_label` lowercases the whole remainder of an unknown phase (e.g. "E2E" renders as "E2e", "API_ERROR" as "Api_error"). The function is the only human-facing phase renderer: `src/evomesh/contracts.py` line 574 f-strings `phase_label(self.phase)` into `CycleOutcome.__repr__`.
   -    >         return text[:1].upper() + text[1:] or "Unknown"
   -    1. [ ] src/evomesh/phase_label.py `phase_label` -- in the unknown-phase fallback, change the line that capitalizes only the first character to instead capitalize the first character and leave the rest of the string untouched.
   +- [ ] Fix `phase_label` to return `None` for an empty/whitespace/`None` phase, matching its documented contract ("empty or `None` -> `None`")
   +    `pha
   ...
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
