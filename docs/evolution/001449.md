# Generation 1449

- **Opened:** 2026-09-24 18:57 UTC
- **Parent generation:** 1447
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The test directly exercises the code under test — `agent_label("trader")` returns `"Trader"` per `_AGENT_LABELS` — which is the simplest realistic call and the one real, observable behavior, matching the style of the analogous `test_phase_label.py`/`test_verdict_label.py` without inventing any mocks.

*Standing goal: Write ONE small, mechanical test for `agent_label()` in `src/evomesh/agent_label.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `agent_label` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: Render an agent role as a short human label.*

### What it changed, and the reason it gave

1. **Change to `tests/test_agent_label.py`** — The test directly exercises the code under test — `agent_label("trader")` returns `"Trader"` per `_AGENT_LABELS` — which is the simplest realistic call and the one real, observable behavior, matching the style of the analogous `test_phase_label.py`/`test_verdict_label.py` without inventing any mocks.

   ```diff
   --- a/tests/test_agent_label.py
   +++ b/tests/test_agent_label.py
   @@ -0,0 +1,5 @@
   +from evomesh.agent_label import agent_label
   +
   +
   +def test_agent_label_maps_a_known_role_to_its_short_label():
   +    assert agent_label("trader") == "Trader"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
