# Generation 1359

- **Opened:** 2026-09-24 04:47 UTC
- **Parent generation:** 1357
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I added two small, mechanical tests to the existing `tests/test_codebase.py` that call the real `known_dead` function — one asserting the empty `frozenset()` result when no baseline file exists, and one asserting correct reading/comment-skipping against a real baseline file — since the function had no direct coverage under tests/ despite being load-bearing.

*Standing goal: Write ONE small, mechanical test for `known_dead()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Read the real definition first -- do not guess its signature or behavior from the name alone. Call `known_dead` with the simplest realistic arguments and assert the one obvious thing about its result. Do not try to cover every branch or every edge case -- one real, passing check that exercises actual behavior is a complete answer.
Do not invent a mock or stub class from scratch. If this needs a stand-in for a dependency, search tests/ first for one that already exists and reuse it -- a test that references something imagined is worse than no test at all.
Add it to the existing test file for this module if one exists (tests/test_codebase.py), or create one if it does not.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — I added two small, mechanical tests to the existing `tests/test_codebase.py` that call the real `known_dead` function — one asserting the empty `frozenset()` result when no baseline file exists, and one asserting correct reading/comment-skipping against a real baseline file — since the function had no direct coverage under tests/ despite being load-bearing.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,3 +1,3 @@
   -from evomesh.codebase import PACKAGE, package_root, survey
   +from evomesh.codebase import PACKAGE, package_root, survey, known_dead
   ```

2. **Change to `tests/test_codebase.py`** — I added two small, mechanical tests to the existing `tests/test_codebase.py` that call the real `known_dead` function — one asserting the empty `frozenset()` result when no baseline file exists, and one asserting correct reading/comment-skipping against a real baseline file — since the function had no direct coverage under tests/ despite being load-bearing.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -12,2 +12,6 @@
        base = next(module for module in survey(project_root) if module.name == "codebase")
        assert base.path == package_root(project_root) / "codebase.py"
   +
   +
   +def test_known_dead_returns_empty_when_no_baseline_file(tmp_path):
   +    assert known_dead(tmp_path) == frozenset()
   ```

3. **Change to `tests/test_codebase.py`** — I added two small, mechanical tests to the existing `tests/test_codebase.py` that call the real `known_dead` function — one asserting the empty `frozenset()` result when no baseline file exists, and one asserting correct reading/comment-skipping against a real baseline file — since the function had no direct coverage under tests/ despite being load-bearing.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -16,2 +16,9 @@
    def test_known_dead_returns_empty_when_no_baseline_file(tmp_path):
        assert known_dead(tmp_path) == frozenset()
   +
   +
   +def test_known_dead_read_and_skips_comments(tmp_path):
   +    baseline = tmp_path / "docs" / "evolution" / "known-dead-modules.txt"
   +    baseline.parent.mkdir(parents=True)
   +    baseline.write_text("# stale orphans\nreachability\n\n\norchestration.py\n")
   +    assert known_dead(tmp_path) == frozenset({"reachability", "orchestration.py"})
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
