# Generation 469

- **Opened:** 2026-09-14 13:27 UTC
- **Parent generation:** 417
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## How it was planned

- **Plan draft** — approved

  > ...
  > s` matches; everything else is separate work for a
  >   future plan.
  > - **Verification before claim.** `explain_not_running()` is only wired if a no-agents
  >   guard actually needs explaining; otherwise it stays dead and that is stated
  >   explicitly. Guessing a call site would fail the next stage.
  > 
  > ## Not done
  > 
  > - No edits to `bdi.py`, `memory.py`, `memory_files.py`, `environment.py`, `models.py`,
  >   or any other load-bearing module.
  > - No renaming of `cycle_agents`/`explain_not_running` to something the caller prefers.
  > - No adding a new module, config, or schema.
  > 
  > ## Verification
  > 
  > - `run_steps()` still advances exactly one cycle per epoch and calls `agent.step(node,
  >   env)` for every agent — confirmed by comparing the two versions line by line and by
  >   running the harness against a small node set (e.g. `python -m evomesh.cli ...` if a
  >   smoke entrypoint exists, or a tiny script that builds a mock environment/nodes/agents
  >   and asserts `cycles.cycle_agents` is the thing being called).
  > - No import errors: `cycles.py` imports cleanly and `run_steps` references only
  >   module-level names after the edit.
  > - The dead-module count drops by one; nothing that imports the loop now silently skips.
  > 
  > RATIONALE: Wire the dead `cycles.py` `cycle_agents(nodes, agents)` into `Agent.run_steps()` in `cognition.py` to replace its inline per-agent `step()` loop (keeping the `self._cyc += 1` update), and only touch `explain_not_running()` if `cognition.py` has an actual no-agents guard that needs explaining.

  - **Reconcile `cycles.py` to the codebase it was named for.** — Make `cycles.py` importable
    - **Load `cycle_agents` into the runtime so `cycles.py` is reachable.** (leaf) — The dead `cycles.py` module exports exactly `cycle_agents()` and `explain_not_running()`. `cycle_agents()` is not a one-line change: it defines the harness-side...
    - **Add `cycles.py` to a new-loads file and remove it from the ratchet dead list.** (leaf) — `known-dead-modules.txt` is a ratchet: *"If a module leaves the dead list because you wired it in, append it to 'new', keeping the file a monotonic history — ne...
  - **Wire a real call site for the real `cycles.py` API.** (leaf) — Only after step 1, and only if a

## Why this change

**What it set out to do.** # Plan: extract the per-agent loop in `cognition.py` to call the live `cycles.py` ## Context `Agent.run_steps()` in `src/evomesh/cognition.py` is the single hot path every agent runs at the top of each harness epoch. It currently inlines the whole per-agent loop: ```python self._cyc += 1 for name in self.nodes: agent = self.agents[name] agent.step(self.nodes[name], self) ``` `cycles.py` (dead, unu...

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/environment.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/environment.py
   +++ b/src/evomesh/environment.py
   @@ -16,4 +16,5 @@
    from evomesh.behaviors import default_behaviors
    from evomesh.cognition import AgentBehavior, CycleOutcome
   +from evomesh.cycles import cycle_agents, explain_not_running
    from evomesh.config import Settings
    from evomesh.contracts import (
   ```

2. **Change to `src/evomesh/environment.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/environment.py
   +++ b/src/evomesh/environment.py
   @@ -16,5 +16,4 @@
    from evomesh.behaviors import default_behaviors
    from evomesh.cognition import AgentBehavior, CycleOutcome
   -from evomesh.cycles import cycle_agents, explain_not_running
    from evomesh.config import Settings
    from evomesh.contracts import (
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
