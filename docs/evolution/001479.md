# Generation 1479

- **Opened:** 2026-09-24 21:06 UTC
- **Parent generation:** 1478
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `ToolsUnsupportedError` subclasses `RuntimeError` with no custom logic, so its one real behavior is standard exception `str()`/`__str__`, which returns the constructor message; I call `str(exc)` on a freshly constructed instance and assert it equals the message, matching the existing construct→call→assert style without any mock.

*Standing goal: Write ONE small, mechanical test for `ToolsUnsupportedError` in `src/evomesh/models.py`. It is exported and load-bearing (used by 9 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Construct one `ToolsUnsupportedError` the way an existing test constructs something similar, call one real method on it, and assert one obvious thing about the result.*

### What it changed, and the reason it gave

1. **Change to `tests/test_models.py`** — `ToolsUnsupportedError` subclasses `RuntimeError` with no custom logic, so its one real behavior is standard exception `str()`/`__str__`, which returns the constructor message; I call `str(exc)` on a freshly constructed instance and assert it equals the message, matching the existing construct→call→assert style without any mock.

   ```diff
   --- a/tests/test_models.py
   +++ b/tests/test_models.py
   @@ -3,4 +3,5 @@
    
    from evomesh.models import (
   +    ToolsUnsupportedError,
        _extract_thought_signature,
        _parse_arguments,
   ```

2. **Change to `tests/test_models.py`** — `ToolsUnsupportedError` subclasses `RuntimeError` with no custom logic, so its one real behavior is standard exception `str()`/`__str__`, which returns the constructor message; I call `str(exc)` on a freshly constructed instance and assert it equals the message, matching the existing construct→call→assert style without any mock.

   ```diff
   --- a/tests/test_models.py
   +++ b/tests/test_models.py
   @@ -60,2 +60,8 @@
    
        assert _tools_are_unsupported(exc) is True
   +
   +
   +def test_tools_unsupported_stores_and_returns_its_message():
   +    exc = ToolsUnsupportedError("take the text protocol instead")
   +
   +    assert str(exc) == "take the text protocol instead"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
