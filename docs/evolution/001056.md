# Generation 1056

- **Opened:** 2026-09-20 13:57 UTC
- **Parent generation:** 1054
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The model changed `src/evomesh/mesh.py` but gave no rationale for it -- see the diff below for what actually moved.

*Standing goal: Wire src/evomesh/cycles.py into the running mesh, or delete it if it is not worth keeping -- both are a complete answer. Nothing imports this module right now, so none of its code ever executes.
What it exports, verbatim -- call one of these, never a name that sounds plausible but is not here: CycleResult, CycleDetector, cycle_agents(), explain_not_running().
Its own docstring says what it is for: Cycle detection for the agent dependency graph.
A natural place to call it from is contracts.py, the most-used module in the package (used by 16 others).*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/mesh.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/mesh.py
   +++ b/src/evomesh/mesh.py
   @@ -53,2 +53,27 @@
            )
            return len(directed_pair_count(edges))
   +
   +    async def run(self) -> None:
   +        """Run the mesh's agents for a set of cycles.
   +
   +        Before stepping the agents, verify the dependency graph is acyclic: a
   +        mutual dependency (e.g. two agents awaiting each other's signals) would
   +        deadlock the whole mesh. :func:`evomesh.cycles.cycle_agents` detects
   +        such cycles and explains why the mesh is not running.
   +        """
   +        from .cycles import cycle_agents, explain_not_running
   +
   +        cycle = cycle_agents(self)
   +        if not cycle.running:
   +            print(explain_not_running(cycle))
   +            return
   +        for i in range(cycle.max_cycles + 1):
   +            awaiting = [
   +                node.node_id
   +                for node in self.nodes.values()
   +                if not node.done()
   +            ]
   +            if not awaiting:
   +                break
   +            await asyncio.gather(*(run_agent(node.node_id) for node in awaiting))
   +
   ```

2. **Change to `src/evomesh/mesh.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/mesh.py
   +++ b/src/evomesh/mesh.py
   @@ -54,26 +54,2 @@
            return len(directed_pair_count(edges))
    
   -    async def run(self) -> None:
   -        """Run the mesh's agents for a set of cycles.
   -
   -        Before stepping the agents, verify the dependency graph is acyclic: a
   -        mutual dependency (e.g. two agents awaiting each other's signals) would
   -        deadlock the whole mesh. :func:`evomesh.cycles.cycle_agents` detects
   -        such cycles and explains why the mesh is not running.
   -        """
   -        from .cycles import cycle_agents, explain_not_running
   -
   -        cycle = cycle_agents(self)
   -        if not cycle.running:
   -            print(explain_not_running(cycle))
   -            return
   -        for i in range(cycle.max_cycles + 1):
   -            awaiting = [
   -                node.node_id
   -                for node in self.nodes.values()
   -                if not node.done()
   -            ]
   -            if not awaiting:
   -                break
   -            await asyncio.gather(*(run_agent(node.node_id) for node in awaiting))
   -
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
