# Generation 1595

- **Opened:** 2026-09-25 10:06 UTC
- **Parent generation:** 1593
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** In `src/evomesh/tools.py` I added a `type: str = "str"` field plus a `@field_validator("type")` on `ToolParameter` that rejects any value not corresponding to a class Pydantic can validate against (`str`, `int`, `float`, `bool`, `list`, `dict`, `bytes`), raising `TypeError("'<name>' 'type' is not a recognized Python type: <value!r>")` — this surfaces malformed values (like `striiing` or `None`) at parse time in `parse_tool` (re-wrapped as `InvalidToolError` by the existing `except (TypeError, ValueError)` block) instead of later as an opaque runtime error. I also added `field_validator` to the pydantic import and added two parametrized cases to `tests/test_tools.py` for `type=None` and `type='striiing'`. Per the instructions I did not edit docs/evolution/improvements.md, did not create any module under src/evomesh/, and removed the stray `diag.py` file from the repo root.

*Standing goal: Implement this improvement to EvoMesh: Validate a tool parameter's declared `type` when parsing the tool, rejecting unrecognized ones [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/tools.py, `ToolParameter`: after the existing fields, add a Pydantic validator that raises `TypeError("... 'type' is not a recognized Python type: ...")` when `type` isn't the name of a class that Pydantic can validate against (e.g. a value of `type='striiing'` or `type=None` is rejected at parse time).
Why the item exists (context, not your task): `parse_tool` reads each parameter's `type` from the tool's YAML and stores it on the `ToolParameter` model, but `ToolParameter` has no validation on it, so a malformed value (a typo like `striiing`, or `None`) is accepted silently and only surfaces later as an opaque pydantic error at runtime in `harness_tools.validator.validate_python`.
> `parameters = [ToolParameter.model_validate(item) for item in raw_parameters]`
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.

The last attempt at this exact target failed. Read this before trying again, so you do not repeat it:
`evomesh codebase hygiene check` failed:
stray file(s) in the repository root: diag.py. Wire the module into something that already imports and runs it, or delete the file -- a candidate cannot land code nothing executes.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/tools.py`** — In `src/evomesh/tools.py` I added a `type: str = "str"` field plus a `@field_validator("type")` on `ToolParameter` that rejects any value not corresponding to a class Pydantic can validate against (`str`, `int`, `float`, `bool`, `list`, `dict`, `bytes`), raising `TypeError("'<name>' 'type' is not a recognized Python type: <value!r>")` — this surfaces malformed values (like `striiing` or `None`) at parse time in `parse_tool` (re-wrapped as `InvalidToolError` by the existing `except (TypeError, ValueError)` block) instead of later as an opaque runtime error. I also added `field_validator` to the pydantic import and added two parametrized cases to `tests/test_tools.py` for `type=None` and `type='striiing'`. Per the instructions I did not edit docs/evolution/improvements.md, did not create any module under src/evomesh/, and removed the stray `diag.py` file from the repo root.

   ```diff
   --- a/src/evomesh/tools.py
   +++ b/src/evomesh/tools.py
   @@ -54,4 +54,12 @@
        description: str = ""
        required: bool = True
   +    type: str = "str"
   +
   +    @field_validator("type")
   +    @classmethod
   +    def _validate_type(cls, value: str) -> str:
   +        if value in ("str", "int", "float", "bool", "list", "dict", "bytes"):
   +            return value
   +        raise TypeError(f"tool parameter type {value!r} is not a recognized Python type")
   ```

2. **Change to `src/evomesh/tools.py`** — In `src/evomesh/tools.py` I added a `type: str = "str"` field plus a `@field_validator("type")` on `ToolParameter` that rejects any value not corresponding to a class Pydantic can validate against (`str`, `int`, `float`, `bool`, `list`, `dict`, `bytes`), raising `TypeError("'<name>' 'type' is not a recognized Python type: <value!r>")` — this surfaces malformed values (like `striiing` or `None`) at parse time in `parse_tool` (re-wrapped as `InvalidToolError` by the existing `except (TypeError, ValueError)` block) instead of later as an opaque runtime error. I also added `field_validator` to the pydantic import and added two parametrized cases to `tests/test_tools.py` for `type=None` and `type='striiing'`. Per the instructions I did not edit docs/evolution/improvements.md, did not create any module under src/evomesh/, and removed the stray `diag.py` file from the repo root.

   ```diff
   --- a/src/evomesh/tools.py
   +++ b/src/evomesh/tools.py
   @@ -35,5 +35,5 @@
    
    import yaml
   -from pydantic import BaseModel, Field
   +from pydantic import BaseModel, Field, field_validator
    
    logger = logging.getLogger(__name__)
   ```

3. **Change to `src/evomesh/tools.py`** — In `src/evomesh/tools.py` I added a `type: str = "str"` field plus a `@field_validator("type")` on `ToolParameter` that rejects any value not corresponding to a class Pydantic can validate against (`str`, `int`, `float`, `bool`, `list`, `dict`, `bytes`), raising `TypeError("'<name>' 'type' is not a recognized Python type: <value!r>")` — this surfaces malformed values (like `striiing` or `None`) at parse time in `parse_tool` (re-wrapped as `InvalidToolError` by the existing `except (TypeError, ValueError)` block) instead of later as an opaque runtime error. I also added `field_validator` to the pydantic import and added two parametrized cases to `tests/test_tools.py` for `type=None` and `type='striiing'`. Per the instructions I did not edit docs/evolution/improvements.md, did not create any module under src/evomesh/, and removed the stray `diag.py` file from the repo root.

   ```diff
   --- a/src/evomesh/tools.py
   +++ b/src/evomesh/tools.py
   @@ -61,5 +61,7 @@
            if value in ("str", "int", "float", "bool", "list", "dict", "bytes"):
                return value
   -        raise TypeError(f"tool parameter type {value!r} is not a recognized Python type")
   +        raise TypeError(
   +            f"tool parameter type {value!r} is not a recognized Python type"
   +        )
   ```

4. **Change to `src/evomesh/tools.py`** — In `src/evomesh/tools.py` I added a `type: str = "str"` field plus a `@field_validator("type")` on `ToolParameter` that rejects any value not corresponding to a class Pydantic can validate against (`str`, `int`, `float`, `bool`, `list`, `dict`, `bytes`), raising `TypeError("'<name>' 'type' is not a recognized Python type: <value!r>")` — this surfaces malformed values (like `striiing` or `None`) at parse time in `parse_tool` (re-wrapped as `InvalidToolError` by the existing `except (TypeError, ValueError)` block) instead of later as an opaque runtime error. I also added `field_validator` to the pydantic import and added two parametrized cases to `tests/test_tools.py` for `type=None` and `type='striiing'`. Per the instructions I did not edit docs/evolution/improvements.md, did not create any module under src/evomesh/, and removed the stray `diag.py` file from the repo root.

   ```diff
   --- a/src/evomesh/tools.py
   +++ b/src/evomesh/tools.py
   @@ -62,5 +62,5 @@
                return value
            raise TypeError(
   -            f"tool parameter type {value!r} is not a recognized Python type"
   +            f"'{name}' 'type' is not a recognized Python type: {value!r}"
            )
   ```

5. **Repair to `src/evomesh/tools.py`** — The `_validate_type` validator is a `@classmethod`, so the name `name` was undefined in its scope; the fix references `cls.__name__` (the class being validated) instead, which is the only valid identifier available there.

   ```diff
   --- a/src/evomesh/tools.py
   +++ b/src/evomesh/tools.py
   @@ -62,5 +62,5 @@
                return value
            raise TypeError(
   -            f"'{name}' 'type' is not a recognized Python type: {value!r}"
   +            f"'{cls.__name__}' 'type' is not a recognized Python type: {value!r}"
            )
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
