# Generation 1481

- **Opened:** 2026-09-24 21:17 UTC
- **Parent generation:** 1480
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The test mirrors the Evolver's construction of a ValidationRun (a held asyncio.Task of ValidationResult) and asserts the code-under-test's observable behavior — running is False once the task is done, and describe() reports the generation and finished state.

*Standing goal: Write ONE small, mechanical test for `ValidationRun` in `src/evomesh/evolution.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Construct one `ValidationRun` the way an existing test constructs something similar, call one real method on it, and assert one obvious thing about the result.*

### What it changed, and the reason it gave

1. **Change to `tests/test_evolution.py`** — The test mirrors the Evolver's construction of a ValidationRun (a held asyncio.Task of ValidationResult) and asserts the code-under-test's observable behavior — running is False once the task is done, and describe() reports the generation and finished state.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -9,8 +9,12 @@
    from __future__ import annotations
    
   +import asyncio
   +
    from evomesh.evolution import (
        GenerationChange,
        ObjectivePick,
        PlanNode,
   +    ValidationResult,
   +    ValidationRun,
        clip,
        decompose_objective,
   ```

2. **Change to `tests/test_evolution.py`** — The test mirrors the Evolver's construction of a ValidationRun (a held asyncio.Task of ValidationResult) and asserts the code-under-test's observable behavior — running is False once the task is done, and describe() reports the generation and finished state.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -100,2 +100,16 @@
        assert change.kind == "mutation"
        assert change.path == "src/evomesh/evolution.py"
   +
   +
   +async def test_validation_run_describes_itself_as_running_then_finished() -> None:
   +    # Build the task the way the Evolver does -- run the suite off-cycle and hold
   +    # the Task -- but here we simply let a finished task stand in for one that ran
   +    # a real validation suite.
   +    task: asyncio.Task[ValidationResult] = asyncio.create_task(
   +        asyncio.sleep(0, result=ValidationResult(passed=True, commands=[]))
   +    )
   +    run = ValidationRun(generation=1, task=task)
   +    await task
   +
   +    assert run.running is False
   +    assert run.describe().endswith("finished")
   ```

3. **Change to `tests/test_evolution.py`** — The test mirrors the Evolver's construction of a ValidationRun (a held asyncio.Task of ValidationResult) and asserts the code-under-test's observable behavior — running is False once the task is done, and describe() reports the generation and finished state.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -113,3 +113,4 @@
    
        assert run.running is False
   -    assert run.describe().endswith("finished")
   +    assert "finished" in run.describe()
   +    assert "validating generation 1" in run.describe()
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
