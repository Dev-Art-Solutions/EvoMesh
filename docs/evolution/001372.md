# Generation 1372

- **Opened:** 2026-09-24 08:53 UTC
- **Parent generation:** 1371
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

*Standing goal: Implement this improvement to EvoMesh: Let a restarted mesh wait briefly for the previous process to release its lock
`SingletonLock.acquire` in src/evomesh/singleton.py fails immediately when the lock is held.
After a promotion the process exits with code 86 and the launcher starts it again at once --
mesh.log shows 25 `another EvoMesh process already holds the lock` refusals, the new process
racing the old one that is still shutting down. Give `acquire` an optional
`wait_seconds: float = 0.0` that retries the non-blocking lock every 0.25s until that much
time has passed before raising `AlreadyRunningError`, and pass a few seconds from the
caller in src/evomesh/__main__.py. The default of 0 keeps today's behavior for everyone else.
It comes from the project's own improvement backlog (docs/evolution/improvements.md). This is a change to how EvoMesh behaves, so it must change at least one file under src/evomesh/ -- a test alone is not an answer, and a candidate that only touches tests or docs is discarded unvalidated. A test covering the new behavior is welcome alongside the source change.
Do not edit docs/evolution/improvements.md yourself: the pipeline ticks this item off once your change is in.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/src/evomesh/singleton.py
   +++ b/src/evomesh/singleton.py
   @@ -17,4 +17,5 @@
    from __future__ import annotations
    
   +import time
    from pathlib import Path
    from typing import BinaryIO
   ```

2. **Change to `src/evomesh/singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/src/evomesh/singleton.py
   +++ b/src/evomesh/singleton.py
   @@ -31,5 +31,5 @@
            self._handle: BinaryIO | None = None
    
   -    def acquire(self) -> None:
   +    def acquire(self, wait_seconds: float = 0.0) -> None:
            if self._handle is not None:
                return
   @@ -39,12 +39,17 @@
            handle = open(self._path, "r+b")
            handle.seek(0)
   -        try:
   -            _lock_exclusive_nonblocking(handle)
   -        except OSError as exc:
   -            handle.close()
   -            raise AlreadyRunningError(
   -                f"another EvoMesh process already holds the lock at {self._path} "
   -                "-- refusing to start a second instance against the same data"
   -            ) from exc
   +        deadline = time.monotonic() + wait_seconds
   +        while True:
   +            try:
   +                _lock_exclusive_nonblocking(handle)
   +                break
   +            except OSError as exc:
   +                if time.monotonic() >= deadline:
   +                    handle.close()
   +                    raise AlreadyRunningError(
   +                        f"another EvoMesh process already holds the lock at {self._path} "
   +                        "-- refusing to start a second i
   ...
   ```

3. **Change to `src/evomesh/__main__.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/src/evomesh/__main__.py
   +++ b/src/evomesh/__main__.py
   @@ -93,5 +93,5 @@
        if lock is not None:
            try:
   -            lock.acquire()
   +            lock.acquire(wait_seconds=5)
            except AlreadyRunningError as exc:
                logger.error(str(exc))
   ```

4. **Change to `tests/test_singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/tests/test_singleton.py
   +++ b/tests/test_singleton.py
   @@ -2,4 +2,5 @@
    
    from pathlib import Path
   +from unittest.mock import patch
    
    import pytest
   ```

5. **Change to `tests/test_singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/tests/test_singleton.py
   +++ b/tests/test_singleton.py
   @@ -2,5 +2,4 @@
    
    from pathlib import Path
   -from unittest.mock import patch
    
    import pytest
   ```

6. **Change to `tests/test_singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/tests/test_singleton.py
   +++ b/tests/test_singleton.py
   @@ -52,2 +52,33 @@
        again.acquire()
        again.release()
   +
   +
   +def test_acquire_waits_and_succeeds_when_lock_released(monkeypatch, tmp_path):
   +    # While the lock is held, every retry must fail with OSError; once the
   +    # holder releases, the next retry must succeed -- proving acquire() retried
   +    # instead of refusing immediately.
   +    holder = SingletonLock(tmp_path / "singleton.lock")
   +    holder.acquire()
   +
   +    calls = {"n": 0}
   +    real_monotonic = __import__("time").monotonic
   +
   +    def fake_monotonic():
   +        calls["n"] += 1
   +        return real_monotonic()
   +
   +    def _lock_exclusive_nonblocking(handle):
   +        # Fail the first two checks, succeed once the holder releases.
   +        if calls["n"] <= 2:
   +            holder.release()
   +            raise OSError("would block")
   +        return True
   +
   +    with patch(
   +        "evomesh.singleton._lock_exclusive_nonblocking",
   +        side_effect=_lock_exclusive_nonblocking,
   +    ), patch("evomesh.singleton.time.monotonic", side_effect=fake_monotonic):
   +        waiter = SingletonLock(tmp_path / "singleton.lock")
   +        waiter.acquire(wait_seconds=5)  # must 
   ...
   ```

7. **Change to `tests/test_singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/tests/test_singleton.py
   +++ b/tests/test_singleton.py
   @@ -54,31 +54,40 @@
    
    
   -def test_acquire_waits_and_succeeds_when_lock_released(monkeypatch, tmp_path):
   -    # While the lock is held, every retry must fail with OSError; once the
   -    # holder releases, the next retry must succeed -- proving acquire() retried
   -    # instead of refusing immediately.
   +def test_acquire_waits_then_succeeds_once_released(tmp_path):
   +    # A real lock is held and then released by a background thread; acquire()
   +    # with a timeout must retry instead of refusing immediately and succeed
   +    # once the holder lets go.
        holder = SingletonLock(tmp_path / "singleton.lock")
        holder.acquire()
    
   -    calls = {"n": 0}
   -    real_monotonic = __import__("time").monotonic
   +    import threading
    
   -    def fake_monotonic():
   -        calls["n"] += 1
   -        return real_monotonic()
   +    release = threading.Event()
    
   -    def _lock_exclusive_nonblocking(handle):
   -        # Fail the first two checks, succeed once the holder releases.
   -        if calls["n"] <= 2:
   -            holder.release()
   -            raise OSError("would block")
   -        return True
   +    def releaser():
   +        time.sleep(0.1)
   +   
   ...
   ```

8. **Change to `tests/test_singleton.py`** — I gave `SingletonLock.acquire` an optional `wait_seconds` parameter that retries the non-blocking flock every up-to-0.25s until the deadline passes before raising `AlreadyRunningError`, and passed `wait_seconds=5` from the `__main__.py` entry point so a restarted mesh process waits out the old one's shutdown instead of racing it and exiting with code 86.

   ```diff
   --- a/tests/test_singleton.py
   +++ b/tests/test_singleton.py
   @@ -1,4 +1,5 @@
    from __future__ import annotations
    
   +import time
    from pathlib import Path
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
