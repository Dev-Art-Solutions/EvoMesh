# Generation 1550

- **Opened:** 2026-09-25 05:33 UTC
- **Parent generation:** 1549
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** In `src/evomesh/phase_label.py`, `phase_label`'s `except KeyError` fallback previously called `.capitalize()`, which lowercased everything after the first character (mangling strings like `"E2E"`→`"E2e"` and `"2D"`→`"2d"`); I replaced it with a first-char-only upper-case transform (`text[:1].upper() + text[1:]`) so trailing digits/uppercase are preserved, and added tests covering the new behavior.

*Standing goal: Implement this improvement to EvoMesh: Capitalize only the first letter when rendering an unknown phase, not the whole string [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/phase_label.py, `phase_label`: in the `except KeyError` fallback, replace `.capitalize()` with an upper-case-only-first-char transform (e.g. `text[:1].upper() + text[1:]`) so trailing digits/uppercase are preserved.
Why the item exists (context, not your task): `phase_label` mangles any phase string that contains uppercase letters or digits: `"E2E"` becomes `"E2e"`, `"2D"` becomes `"2d"`, because the fallback calls `.capitalize()`, which lowercases everything after the first character. That fallback exists precisely to render phases not in `_LABELS`, and the function is typed and called with arbitrary phase strings.
>         return text.replace("_", " ").strip().capitalize() or "Unknown"
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/phase_label.py`** — In `src/evomesh/phase_label.py`, `phase_label`'s `except KeyError` fallback previously called `.capitalize()`, which lowercased everything after the first character (mangling strings like `"E2E"`→`"E2e"` and `"2D"`→`"2d"`); I replaced it with a first-char-only upper-case transform (`text[:1].upper() + text[1:]`) so trailing digits/uppercase are preserved, and added tests covering the new behavior.

   ```diff
   --- a/src/evomesh/phase_label.py
   +++ b/src/evomesh/phase_label.py
   @@ -45,3 +45,3 @@
            return _LABELS[text]
        except KeyError:
   -        return text.replace("_", " ").strip().capitalize() or "Unknown"+        return text[:1].upper() + text[1:].replace("_", " ").strip() or "Unknown"
   ```

2. **Change to `src/evomesh/phase_label.py`** — In `src/evomesh/phase_label.py`, `phase_label`'s `except KeyError` fallback previously called `.capitalize()`, which lowercased everything after the first character (mangling strings like `"E2E"`→`"E2e"` and `"2D"`→`"2d"`); I replaced it with a first-char-only upper-case transform (`text[:1].upper() + text[1:]`) so trailing digits/uppercase are preserved, and added tests covering the new behavior.

   ```diff
   --- a/src/evomesh/phase_label.py
   +++ b/src/evomesh/phase_label.py
   @@ -45,3 +45,4 @@
            return _LABELS[text]
        except KeyError:
   -        return text[:1].upper() + text[1:].replace("_", " ").strip() or "Unknown"+        text = text.replace("_", " ").strip()
   +        return text[:1].upper() + text[1:] or "Unknown"
   ```

3. **Change to `tests/test_phase_label.py`** — In `src/evomesh/phase_label.py`, `phase_label`'s `except KeyError` fallback previously called `.capitalize()`, which lowercased everything after the first character (mangling strings like `"E2E"`→`"E2e"` and `"2D"`→`"2d"`); I replaced it with a first-char-only upper-case transform (`text[:1].upper() + text[1:]`) so trailing digits/uppercase are preserved, and added tests covering the new behavior.

   ```diff
   --- a/tests/test_phase_label.py
   +++ b/tests/test_phase_label.py
   @@ -10,2 +10,13 @@
    
        assert phase_label(AgentPhase.IDLE) == "Idle"
   +
   +
   +def test_phase_label_preserves_trailing_uppercase_and_digits():
   +    # Unknown phases: only the first char is upper-cased; the rest is
   +    # left as-is, so "E2E" and "2D" are not mangled.
   +    assert phase_label("E2E") == "E2E"
   +    assert phase_label("2D") == "2D"
   +
   +
   +def test_phase_label_capitalizes_only_the_first_char():
   +    assert phase_label("planning_phase") == "Planning phase"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
