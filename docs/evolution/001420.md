# Generation 1420

- **Opened:** 2026-09-24 15:49 UTC
- **Parent generation:** 1419
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Added `start_new_session=True` to the `subprocess.run(...)` call in `run_command` so the child process is placed in its own new session/process group (via setsid), separate from the worker thread — the single change requested for this step, setting up later steps to target the group with `os.killpg`.

*Standing goal: Implement this improvement to EvoMesh: Kill the whole process group when run_command's timeout fires [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/processes.py, `run_command`: add `start_new_session=True` to the `subprocess.run(...)` call so the child leads its own process group/session, separate from the worker thread.
Why the item exists (context, not your task): `run_command` is meant to run an external command "without leaving a transport behind," but on timeout it kills only the direct child; any grandchild the child spawned (a backgrounded process, a process group) becomes orphaned and keeps running. The `subprocess.run(...)` call has no `start_new_session`/`preexec_fn`, and the `except subprocess.TimeoutExpired` handler returns a fixed result without touching the group.
>             completed = subprocess.run(  # noqa: S603 - the caller supplies the program
>                 [program, *arguments],
>                 cwd=str(cwd) if cwd else None,
 [...]
Later steps, NOT this one: 2. src/evomesh/processes.py `run_command` -- in the `except subprocess.TimeoutExpired` handler, terminate that whole process group (via `os.killpg`) before returning the timeout result, so children/grandchildren don't outlive the parent.
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/processes.py`** — Added `start_new_session=True` to the `subprocess.run(...)` call in `run_command` so the child process is placed in its own new session/process group (via setsid), separate from the worker thread — the single change requested for this step, setting up later steps to target the group with `os.killpg`.

   ```diff
   --- a/src/evomesh/processes.py
   +++ b/src/evomesh/processes.py
   @@ -78,4 +78,5 @@
                    check=False,
                    env=env,
   +                start_new_session=True,
                )
                return completed.returncode, completed.stdout or b"", False
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
