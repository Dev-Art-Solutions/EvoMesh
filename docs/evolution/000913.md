# Generation 913

- **Opened:** 2026-09-18 19:21 UTC
- **Parent generation:** 479
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## How it was planned

- **Plan draft** — approved

  > ...
  > airs to exactly one undirected edge — the helper dedups both directions.
  > - `directed_pair_count` never mutates the edges it is handed; calling `edge_count()`
  >   twice on the same mesh returns the same number with no observable side effect.
  > 
  > ## How to split this into smaller work items (for the next generation)
  > 
  > 1. Add the single `edge_count(self) -> int` method to `Mesh` in
  >    `src/evomesh/mesh.py`, calling `directed_pair_count` from `mesh_utils.py`.
  >    No import of mesh_utils into mesh.py is required — the method can call the
  >    helper by importing it locally inside the method body, or the plan may note
  >    that if the architecture forbids a top-level import, a module-level import in
  >    `mesh.py` of the single private helper is the minimal dependency edge. Either
  >    way the dependency is one-directional and stays in this file.
  > 2. Add a small test asserting the three behaviors above (empty mesh, one edge,
  >    both-directions dedup via `undirected_edges`).
  > 3. After step 1 and 2 pass, decide whether `merge_attributes` and
  >    `build_adjacency` warrant their own Mesh methods; if so, wire them in the
  >    same manner in a follow-up generation, not in this one.
  > 
  > RATIONALE: Wire the dead `directed_pair_count` helper from `src/evomesh/mesh_utils.py` into the live `Mesh` class as a new `edge_count()` method, then add a test covering the empty/one-edge/both-directions cases, leaving `undirected_edges` and `merge_attributes` as follow-up wires and `cycles.py`/`node.py` for later generations.

  - **Add one `edge_count(self) -> int` method to the `Mesh` dataclass in `src/evomesh/mesh.py` that calls `directed_pair_count` from `mesh_utils.py`; the wire is one-directional (mesh.py consumes mesh_utils.py) and touches only the load-bearing module.** (leaf) — Add Mesh.edge_count wiring into mesh.py.
  - **Add a small test asserting `Mesh().edge_count() == 0`, one edge => 1, and that both-direction edges collapse via `undirected_edges`; depends on 1.** (leaf) — Test that Mesh.edge_count() is correct.

## Why this change

**What it set out to do.** # Plan: expose a Mesh.edge_count() backed by the mesh_utils graph helpers ## What this candidate changes and what it does not Wires the currently-dead `src/evomesh/mesh_utils.py` into the live mesh by giving the `Mesh` class a public edge-count method. It does **not** touch `cycles.py` or `node.py` (those belong to later generations), and it does **not** create a new graph module. Specifically, on...

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/mesh.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/mesh.py
   +++ b/src/evomesh/mesh.py
   @@ -49,2 +49,10 @@
                for target, label in targets.items():
                    yield source, target, label
   +
   +    def edge_count(self) -> int:
   +        edges = (
   +            (source, target)
   +            for source, targets in self.edges.items()
   +            for target in targets
   +        )
   +        return len(directed_pair_count(edges))
   ```

2. **Change to `src/evomesh/mesh.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/mesh.py
   +++ b/src/evomesh/mesh.py
   @@ -3,4 +3,6 @@
    from collections.abc import Iterable
    from dataclasses import dataclass, field
   +
   +from .mesh_utils import directed_pair_count
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
