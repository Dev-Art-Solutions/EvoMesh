# Generation 1431

- **Opened:** 2026-09-24 16:58 UTC
- **Parent generation:** 1428
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `parse_plan_children` splits a decompose node's text into child dicts keyed by `title`, `reasoning`, and `depends_on`; the simplest real input `"- title one :: reason one"` yields a list whose first element has `title == "title one"`, which is the single obvious, load-bearing behavior the function performs — so I added one import and one assertion that captures exactly that.

*Standing goal: Write ONE small, mechanical test for `parse_plan_children()` in `src/evomesh/evolution.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `parse_plan_children` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.*

### What it changed, and the reason it gave

1. **Change to `tests/test_evolution.py`** — `parse_plan_children` splits a decompose node's text into child dicts keyed by `title`, `reasoning`, and `depends_on`; the simplest real input `"- title one :: reason one"` yields a list whose first element has `title == "title one"`, which is the single obvious, load-bearing behavior the function performs — so I added one import and one assertion that captures exactly that.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -15,4 +15,5 @@
        evaluate_plan_objective,
        harness_objective,
   +    parse_plan_children,
        parse_plan_verdict,
        review_objective,
   ```

2. **Change to `tests/test_evolution.py`** — `parse_plan_children` splits a decompose node's text into child dicts keyed by `title`, `reasoning`, and `depends_on`; the simplest real input `"- title one :: reason one"` yields a list whose first element has `title == "title one"`, which is the single obvious, load-bearing behavior the function performs — so I added one import and one assertion that captures exactly that.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -53,2 +53,8 @@
        assert "add a new module" in prompt
        assert "evomesh" in prompt
   +
   +
   +def test_parse_plan_children_returns_the_first_child_title() -> None:
   +    result = parse_plan_children("- title one :: reason one")
   +    assert result is not None
   +    assert result[0]["title"] == "title one"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
