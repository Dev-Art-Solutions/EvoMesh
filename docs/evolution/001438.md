# Generation 1438

- **Opened:** 2026-09-24 17:39 UTC
- **Parent generation:** 1437
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The test calls the load-bearing, untested `humanize_size()` with a realistic sub-1024 input and asserts exactly what the code shows it does (whole-number byte rendering), a single passing check covering the byte branch without duplicating the existing KiB-decimal test.

*Standing goal: Write ONE small, mechanical test for `humanize_size()` in `src/evomesh/humanize.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `humanize_size` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: Human-friendly formatting helpers for EvoMesh.*

### What it changed, and the reason it gave

1. **Change to `tests/test_humanize.py`** — The test calls the load-bearing, untested `humanize_size()` with a realistic sub-1024 input and asserts exactly what the code shows it does (whole-number byte rendering), a single passing check covering the byte branch without duplicating the existing KiB-decimal test.

   ```diff
   --- a/tests/test_humanize.py
   +++ b/tests/test_humanize.py
   @@ -1,5 +1,5 @@
    from datetime import UTC, datetime, timedelta
    
   -from evomesh.humanize import humanize_bytes, humanize_duration, humanize_timestamp
   +from evomesh.humanize import humanize_bytes, humanize_duration, humanize_size, humanize_timestamp
   ```

2. **Change to `tests/test_humanize.py`** — The test calls the load-bearing, untested `humanize_size()` with a realistic sub-1024 input and asserts exactly what the code shows it does (whole-number byte rendering), a single passing check covering the byte branch without duplicating the existing KiB-decimal test.

   ```diff
   --- a/tests/test_humanize.py
   +++ b/tests/test_humanize.py
   @@ -21,2 +21,8 @@
        result = humanize_duration(0.5)
        assert result == "500 ms"
   +
   +
   +def test_humanize_size_sub_1024_renders_as_whole_bytes():
   +    # Below one KiB the value stays in bytes and renders as a whole number, not a decimal.
   +    result = humanize_size(512)
   +    assert result == "512 B"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
