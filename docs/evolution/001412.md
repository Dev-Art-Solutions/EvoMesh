# Generation 1412

- **Opened:** 2026-09-24 15:13 UTC
- **Parent generation:** 1411
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** A single passing assertion against the exact output (verified by reading the source, not a mock) fully covers `plan_objective()` without inventing stubs or changing production code.

*Standing goal: Write ONE small, mechanical test for `plan_objective()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `plan_objective` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — A single passing assertion against the exact output (verified by reading the source, not a mock) fully covers `plan_objective()` without inventing stubs or changing production code.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,3 +1,4 @@
    from evomesh.codebase import (
   +    IMPROVEMENTS_FILE,
        PACKAGE,
        Improvement,
   @@ -6,4 +7,5 @@
        package_root,
        plan_needle,
   +    plan_objective,
        stray_root_files,
        survey,
   ```

2. **Change to `tests/test_codebase.py`** — A single passing assertion against the exact output (verified by reading the source, not a mock) fully covers `plan_objective()` without inventing stubs or changing production code.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -51,2 +51,14 @@
            "Plan this improvement to EvoMesh: Wire up `known_dead"
        )
   +
   +
   +def test_plan_objective_uses_the_item_title_on_its_own_line():
   +    assert plan_objective(Improvement("Split the DB module")) == "\n".join(
   +        [
   +            "Plan this improvement to EvoMesh: Split the DB module",
   +            f"Split this backlog item into 1 to 3 small steps and write them under it "
   +            f"in {IMPROVEMENTS_FILE.as_posix()}. Do not change any code: each step "
   +            "becomes one later generation's whole objective.",
   +            "THE ITEM: Split the DB module",
   +        ]
   +    )
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
