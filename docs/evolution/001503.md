# Generation 1503

- **Opened:** 2026-09-25 00:40 UTC
- **Parent generation:** 1502
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The current code at lines 70-71 catches the `TimeoutError` from `asyncio.wait_for(self._tick(), ...)` but logs a bare message with no exception info, so the timed-out commands the mesh logged leave no trace of what failed; logging `exc=e` records the cause without changing the timeout, process-group, or result-reporting behavior that the existing timeout-kill test depends on.

*Standing goal: Refill the improvement backlog from src/evomesh/watchers.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/watchers.py and write it down as one new item with its steps -- do not fix it.
What the module is for: A deterministic watcher: a command run on its own short, fixed interval,
What the running mesh logged from it since the file last changed (numbers masked) -- the strongest lead there is:
  - 3x Watcher command timed out: ['python', 'D:\\Projects\\Dev-art solutions\\EvoMesh\\agent-templates\\news-watcher/scripts/watch_news.py']
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit

The last attempt at this exact target failed. Read this before trying again, so you do not repeat it:
it made no real edit at all -- the candidate was discarded before anything could even be validated.*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — The current code at lines 70-71 catches the `TimeoutError` from `asyncio.wait_for(self._tick(), ...)` but logs a bare message with no exception info, so the timed-out commands the mesh logged leave no trace of what failed; logging `exc=e` records the cause without changing the timeout, process-group, or result-reporting behavior that the existing timeout-kill test depends on.

   ```diff
   +- [ ] Log the exception detail when a watcher command times out
   +    The watchdog log line is empty of cause: the mesh logged "Watcher command timed out" but the handler that emits it captures the `TimeoutError` yet logs nothing about it, so operators can't tell what actually failed. This is a distinct gap from the already-done timeout-kill fix — nothing about logging the cause has been addressed.
   +    >             except TimeoutError:
   +    >                 logger.warning("Watcher command timed out: %s", self._argv)
   +    1. [ ] src/evomesh/watchers.py `AgentWatcher._loop` -- add `exc=e` to the timeout log so the `TimeoutError` (with its traceback/cause) is recorded.
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
