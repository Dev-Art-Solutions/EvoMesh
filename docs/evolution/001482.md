# Generation 1482

- **Opened:** 2026-09-24 21:22 UTC
- **Parent generation:** 1481
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The watcher is deterministic and runs on a short fixed interval, so its timeout must be tunable per watcher via settings; the current hard-coded `DEFAULT_TIMEOUT_SECONDS = 20.0` with no override path makes long-running commands like the news-watcher (which `sleep(60)` in its loop) time out on every cycle.

*Standing goal: Refill the improvement backlog from src/evomesh/watchers.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/watchers.py and write it down as one new item with its steps -- do not fix it.
What the module is for: A deterministic watcher: a command run on its own short, fixed interval,
What the running mesh logged from it since the file last changed (numbers masked) -- the strongest lead there is:
  - 2x Watcher command timed out: ['python', 'D:\\Projects\\Dev-art solutions\\EvoMesh\\agent-templates\\news-watcher/scripts/watch_news.py']
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — The watcher is deterministic and runs on a short fixed interval, so its timeout must be tunable per watcher via settings; the current hard-coded `DEFAULT_TIMEOUT_SECONDS = 20.0` with no override path makes long-running commands like the news-watcher (which `sleep(60)` in its loop) time out on every cycle.

   ```diff
   +- [ ] Set the watcher's command timeout from settings instead of the hardcoded 20s default
   +    The `AgentWatcher.__init__` accepts an optional `timeout_seconds` but defaults it to `DEFAULT_TIMEOUT_SECONDS = 20.0`, and no caller in `mesh.py` ever passes it — so a watcher with no settings-backed timeout override is stuck at 20s. The running mesh logged two `timed_out` results for the exact command `['python', '...\\agent-templates\\news-watcher\\scripts\\watch_news.py']`; that script cycles on `while True:` with `time.sleep(60)`, so it can never finish within 20s and will always time out.
   +    > timeout_seconds: float = DEFAULT_TIMEOUT_SECONDS,
   +    1. [ ] src/evomesh/watchers.py `AgentWatcher.__init__` -- read `timeout_seconds` out of the watcher settings (defaulting to `DEFAULT_TIMEOUT_SECONDS` only when unset) instead of hard-coding the module constant, and pass it through from `Mesh.ensure_watcher` so the per-watcher command timeout comes from config, not a fixed number.
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
