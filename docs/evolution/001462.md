# Generation 1462

- **Opened:** 2026-09-24 19:39 UTC
- **Parent generation:** 1460
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The class is literally `class TelegramError(RuntimeError): pass`, so it has no own methods — its only observable behavior is inherited `RuntimeError` message storage, which `str()` exercises; the test constructs it with a real argument and asserts `str()` returns it, and I left `src/evomesh/` untouched.

*Standing goal: Write ONE small, mechanical test for `TelegramError` in `src/evomesh/telegram.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Construct one `TelegramError` the way an existing test constructs something similar, call one real method on it, and assert one obvious thing about the result.
The module's own docstring says what it is for: A Telegram bot as a second console onto the same running mesh.*

### What it changed, and the reason it gave

1. **Change to `tests/test_telegram.py`** — The class is literally `class TelegramError(RuntimeError): pass`, so it has no own methods — its only observable behavior is inherited `RuntimeError` message storage, which `str()` exercises; the test constructs it with a real argument and asserts `str()` returns it, and I left `src/evomesh/` untouched.

   ```diff
   --- a/tests/test_telegram.py
   +++ b/tests/test_telegram.py
   @@ -0,0 +1,8 @@
   +"""A Telegram bot as a second console onto the same running mesh."""
   +
   +from evomesh.telegram import TelegramError
   +
   +
   +def test_telegram_error_stores_its_message_like_a_runtime_error() -> None:
   +    error = TelegramError("bot is unreachable")
   +    assert str(error) == "bot is unreachable"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
