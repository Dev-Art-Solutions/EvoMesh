# Generation 1428

- **Opened:** 2026-09-24 16:48 UTC
- **Parent generation:** 1427
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** In `src/evomesh/watchers.py`, `AgentWatcher._tick` now passes `timeout_seconds=self.timeout_seconds` into the `run_command` call so `subprocess.run` terminates the child itself on timeout (turning the timeout into a real kill rather than a leaked waiter); the `_loop` `wait_for` is left as a safety net. Added a test in `tests/test_watchers.py` asserting a 30s child is killed by a 1s timeout and `result.timed_out` is set.

*Standing goal: Implement this improvement to EvoMesh: Make the watcher's timeout actually kill the command, not just stop awaiting it [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/watchers.py, `AgentWatcher._tick`: pass `timeout_seconds=self.timeout_seconds` into the `run_command` call so `subprocess.run` terminates the child itself on timeout (via its `timeout=`), turning the child into a real stop rather than a leaked waiter; the `wait_for` in `_loop` then serves only as a safety net.
Why the item exists (context, not your task): `AgentWatcher._tick` calls `run_command` with no `timeout_seconds`, and `_loop` relies solely on `asyncio.wait_for` to enforce `self.timeout_seconds`. Per the `run_command` docstring (processes.py:58-67), cancelling the await on the worker thread does not stop the blocking `subprocess.run` running inside that thread, so a timed-out child keeps running for real — the watcher's own timeout never actually terminates the leaked process.
> `await asyncio.wait_for(self._tick(), timeout=self.timeout_seconds)`
> `result = await run_command(self._argv[0], *self._argv[1:], cwd=self.cwd)`
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/watchers.py`** — In `src/evomesh/watchers.py`, `AgentWatcher._tick` now passes `timeout_seconds=self.timeout_seconds` into the `run_command` call so `subprocess.run` terminates the child itself on timeout (turning the timeout into a real kill rather than a leaked waiter); the `_loop` `wait_for` is left as a safety net. Added a test in `tests/test_watchers.py` asserting a 30s child is killed by a 1s timeout and `result.timed_out` is set.

   ```diff
   --- a/src/evomesh/watchers.py
   +++ b/src/evomesh/watchers.py
   @@ -75,5 +75,7 @@
    
        async def _tick(self) -> None:
   -        result = await run_command(self._argv[0], *self._argv[1:], cwd=self.cwd)
   +        result = await run_command(
   +            self._argv[0], *self._argv[1:], cwd=self.cwd, timeout_seconds=self.timeout_seconds
   +        )
            message = result.output.strip()
            if result.exit_code != 0:
   ```

2. **Change to `tests/test_watchers.py`** — In `src/evomesh/watchers.py`, `AgentWatcher._tick` now passes `timeout_seconds=self.timeout_seconds` into the `run_command` call so `subprocess.run` terminates the child itself on timeout (turning the timeout into a real kill rather than a leaked waiter); the `_loop` `wait_for` is left as a safety net. Added a test in `tests/test_watchers.py` asserting a 30s child is killed by a 1s timeout and `result.timed_out` is set.

   ```diff
   --- a/tests/test_watchers.py
   +++ b/tests/test_watchers.py
   @@ -53,4 +53,28 @@
    
    
   +async def test_a_hanging_child_is_killed_by_the_watcher_timeout() -> None:
   +    # The timeout must reach run_command's own subprocess.run(timeout=...) so the
   +    # child is killed by that call, not just leaked while the await is cancelled.
   +    import time
   +
   +    async def notify(_: str) -> None:
   +        return None
   +
   +    watcher = AgentWatcher(
   +        f'{PYTHON} -c "import time; time.sleep(30)"',
   +        interval_seconds=1,
   +        notify=notify,
   +        timeout_seconds=1.0,
   +    )
   +    watcher.start()
   +    started = time.monotonic()
   +    try:
   +        await watcher.run_once()
   +    finally:
   +        await watcher.stop()
   +
   +    assert time.monotonic() - started < 10  # child's real sleep is 30s
   +
   +
    async def test_stop_cancels_the_loop() -> None:
        async def notify(_: str) -> None:
   ```

3. **Change to `tests/test_watchers.py`** — In `src/evomesh/watchers.py`, `AgentWatcher._tick` now passes `timeout_seconds=self.timeout_seconds` into the `run_command` call so `subprocess.run` terminates the child itself on timeout (turning the timeout into a real kill rather than a leaked waiter); the `_loop` `wait_for` is left as a safety net. Added a test in `tests/test_watchers.py` asserting a 30s child is killed by a 1s timeout and `result.timed_out` is set.

   ```diff
   --- a/tests/test_watchers.py
   +++ b/tests/test_watchers.py
   @@ -67,12 +67,11 @@
            timeout_seconds=1.0,
        )
   -    watcher.start()
   +
        started = time.monotonic()
   -    try:
   -        await watcher.run_once()
   -    finally:
   -        await watcher.stop()
   +    result = await watcher._tick()  # the child's real sleep is 30s
   +    assert result.timed_out
    
   -    assert time.monotonic() - started < 10  # child's real sleep is 30s
   +    assert time.monotonic() - started < 10  # if the timeout never reached
   +    # subprocess.run, the child would leak and the real 30s sleep would hang us out.
   ```

4. **Repair to `src/evomesh/watchers.py`** — Pyright flagged `result.timed_out` because `_tick` was declared to return `None`, so `result` was `None`. I changed `_tick` to return the `CommandResult` it already obtained from `run_command` (in both the non-zero exit and normal paths), added the `CommandResult` to the import, and changed the return type annotation to `CommandResult` — making `result.timed_out` a valid access with the minimal change.

   ```diff
   --- a/src/evomesh/watchers.py
   +++ b/src/evomesh/watchers.py
   @@ -74,5 +74,5 @@
                await asyncio.sleep(self.interval_seconds)
    
   -    async def _tick(self) -> None:
   +    async def _tick(self) -> CommandResult:
            result = await run_command(
                self._argv[0], *self._argv[1:], cwd=self.cwd, timeout_seconds=self.timeout_seconds
   @@ -81,5 +81,6 @@
            if result.exit_code != 0:
                logger.warning("Watcher command exited %s: %s", result.exit_code, message[:500])
   -            return
   +            return result
            if message:
                await self.notify(message)
   +        return result
   ```

5. **Repair to `src/evomesh/watchers.py`** — Pyright flagged `result.timed_out` because `_tick` was declared to return `None`, so `result` was `None`. I changed `_tick` to return the `CommandResult` it already obtained from `run_command` (in both the non-zero exit and normal paths), added the `CommandResult` to the import, and changed the return type annotation to `CommandResult` — making `result.timed_out` a valid access with the minimal change.

   ```diff
   --- a/src/evomesh/watchers.py
   +++ b/src/evomesh/watchers.py
   @@ -20,5 +20,5 @@
    from pathlib import Path
    
   -from evomesh.processes import run_command
   +from evomesh.processes import CommandResult, run_command
    
    logger = logging.getLogger(__name__)
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
