# Generation 1456

- **Opened:** 2026-09-24 19:16 UTC
- **Parent generation:** 1455
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** This asserts the single documented behavior of `runtime_fault_needle` (its returned prefix), using the real `RuntimeFault` dataclass with simple field values and no changes under `src/evomesh/`.

*Standing goal: Write ONE small, mechanical test for `runtime_fault_needle()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `runtime_fault_needle` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — This asserts the single documented behavior of `runtime_fault_needle` (its returned prefix), using the real `RuntimeFault` dataclass with simple field values and no changes under `src/evomesh/`.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -3,4 +3,5 @@
        PACKAGE,
        Improvement,
   +    RuntimeFault,
        Step,
        fabricated_references,
   ```

2. **Change to `tests/test_codebase.py`** — This asserts the single documented behavior of `runtime_fault_needle` (its returned prefix), using the real `RuntimeFault` dataclass with simple field values and no changes under `src/evomesh/`.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -88,2 +88,19 @@
            "Implement this improvement to EvoMesh: Split the DB module"
        )
   +
   +
   +def test_runtime_fault_needle_prefixed_with_the_bug_prefix():
   +    assert runtime_fault_needle(
   +        RuntimeFault(
   +            module="codebase",
   +            function="survey",
   +            line=1,
   +            exception="KeyError",
   +            count=1,
   +            last_seen="",
   +            traceback="",
   +        )
   +    ) == (
   +        "Fix a real bug the running mesh hit: `KeyError` escaping "
   +        "`survey` in `src/evomesh/codebase.py`"
   +    )
   ```

3. **Repair to `tests/test_codebase.py`** — The F821 error was because the test file referenced `runtime_fault_needle` without importing it; I added it to the existing import block from `evomesh.codebase`, keeping the isort alphabetical order intact and changing nothing else.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -7,4 +7,5 @@
        fabricated_references,
        improvement_needle,
   +    runtime_fault_needle,
        known_dead,
        package_root,
   ```

4. **Repair to `tests/test_codebase.py`** — The F821 error was because the test file referenced `runtime_fault_needle` without importing it; I added it to the existing import block from `evomesh.codebase`, keeping the isort alphabetical order intact and changing nothing else.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -7,5 +7,4 @@
        fabricated_references,
        improvement_needle,
   -    runtime_fault_needle,
        known_dead,
        package_root,
   ```

5. **Repair to `tests/test_codebase.py`** — The F821 error was because the test file referenced `runtime_fault_needle` without importing it; I added it to the existing import block from `evomesh.codebase`, keeping the isort alphabetical order intact and changing nothing else.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -12,4 +12,5 @@
        plan_objective,
        scout_needle,
   +    runtime_fault_needle,
        step_needle,
        stray_root_files,
   ```

6. **Repair to `tests/test_codebase.py`** — The F821 error was because the test file referenced `runtime_fault_needle` without importing it; I added it to the existing import block from `evomesh.codebase`, keeping the isort alphabetical order intact and changing nothing else.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -11,6 +11,6 @@
        plan_needle,
        plan_objective,
   +    runtime_fault_needle,
        scout_needle,
   -    runtime_fault_needle,
        step_needle,
        stray_root_files,
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
