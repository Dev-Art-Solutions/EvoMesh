# Generation 1494

- **Opened:** 2026-09-24 23:13 UTC
- **Parent generation:** 1486
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Standard cron uses both `0` and `7` for Sunday, the code advertises `7` as valid via `_FIELD_RANGES[4] = (0, 7)`, yet `_day_matches`/`next_after` never fire it — folding `7`→`0` in `parse` is a one-function change that makes accepted `7` expressions actually match.

*Standing goal: Refill the improvement backlog from src/evomesh/cron.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/cron.py and write it down as one new item with its steps -- do not fix it.
What the module is for: A minimal 5-field cron expression matcher.
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — Standard cron uses both `0` and `7` for Sunday, the code advertises `7` as valid via `_FIELD_RANGES[4] = (0, 7)`, yet `_day_matches`/`next_after` never fire it — folding `7`→`0` in `parse` is a one-function change that makes accepted `7` expressions actually match.

   ```diff
   +- [ ] Make cron treat weekday `7` as Sunday
   +    `_FIELD_RANGES` in `src/evomesh/cron.py` declares the weekday field range as `(0, 7)`, so `7` is a valid Sunday value that `parse()` accepts. But `_day_matches` computes `dow_ok` with `(day.weekday() + 1) % 7`, which maps Sunday (Python weekday 6) to `0` and never to `7` — so a weekday set containing only `7` never matches, and `next_after` then gives up with "never matches within four years" instead of firing on Sundays.
   +    > `dow_ok = ((day.weekday() + 1) % 7) in weekday_set`
   +    1. [ ] src/evomesh/cron.py `parse` -- after building `weekday_set` (line 57), fold the Sunday value `7` into `0` (e.g. `if 7 in weekday_set: weekday_set.discard(7); weekday_set.add(0)`) so `0`/`7` are treated identically, keeping the set canonical for `_day_matches`/`next_after`.
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
