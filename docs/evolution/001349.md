# Generation 1349

- **Opened:** 2026-09-24 01:39 UTC
- **Parent generation:** 1347
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I added one small test to the existing `tests/test_behaviors.py` that calls the real, load-bearing `default_behaviors()` with no arguments (its simplest realistic signature, read live from the module) and asserts its result contains exactly the four documented built-in system agent keys with non-None values, giving `default_behaviors` direct test coverage it previously lacked.

*Standing goal: Write ONE small, mechanical test for `default_behaviors()` in `src/evomesh/behaviors.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Read the real definition first -- do not guess its signature or behavior from the name alone. Call `default_behaviors` with the simplest realistic arguments and assert the one obvious thing about its result. Do not try to cover every branch or every edge case -- one real, passing check that exercises actual behavior is a complete answer.
Do not invent a mock or stub class from scratch. If this needs a stand-in for a dependency, search tests/ first for one that already exists and reuse it -- a test that references something imagined is worse than no test at all.
Add it to the existing test file for this module if one exists (tests/test_behaviors.py), or create one if it does not.
The module's own docstring says what it is for: BDI behaviors for the four built-in system agents.*

### What it changed, and the reason it gave

1. **Change to `tests/test_behaviors.py`** — I added one small test to the existing `tests/test_behaviors.py` that calls the real, load-bearing `default_behaviors()` with no arguments (its simplest realistic signature, read live from the module) and asserts its result contains exactly the four documented built-in system agent keys with non-None values, giving `default_behaviors` direct test coverage it previously lacked.

   ```diff
   --- a/tests/test_behaviors.py
   +++ b/tests/test_behaviors.py
   @@ -1,3 +1,3 @@
   -from evomesh.behaviors import _extract_rationale
   +from evomesh.behaviors import _extract_rationale, default_behaviors
    
    RATIONALE_MARKER = "RATIONALE:"
   ```

2. **Change to `tests/test_behaviors.py`** — I added one small test to the existing `tests/test_behaviors.py` that calls the real, load-bearing `default_behaviors()` with no arguments (its simplest realistic signature, read live from the module) and asserts its result contains exactly the four documented built-in system agent keys with non-None values, giving `default_behaviors` direct test coverage it previously lacked.

   ```diff
   --- a/tests/test_behaviors.py
   +++ b/tests/test_behaviors.py
   @@ -7,2 +7,8 @@
        answer = f"{RATIONALE_MARKER} The model chose gold because of rising yields."
        assert _extract_rationale(answer) == "The model chose gold because of rising yields."
   +
   +
   +def test_default_behaviors_returns_the_four_system_agents():
   +    behaviors = default_behaviors()
   +    assert set(behaviors) == {"architect", "guardian", "evaluator", "evolver"}
   +    assert all(value is not None for value in behaviors.values())
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
