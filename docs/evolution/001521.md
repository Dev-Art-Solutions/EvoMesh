# Generation 1521

- **Opened:** 2026-09-25 03:27 UTC
- **Parent generation:** 1520
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Changed the fallback of the `.get` on the final line of `agent_label` in src/evomesh/agent_label.py from the literal `"agent"` to the `role` argument (`return _AGENT_LABELS.get(role, role)`), so custom/unknown agent roles are returned verbatim instead of the generic "agent"; also updated the docstring to match and added a test asserting `agent_label("researcher") == "researcher"`.

*Standing goal: Implement this improvement to EvoMesh: Make `agent_label` return the role itself for roles absent from `_AGENT_LABELS` instead of the hard-coded "agent" [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/agent_label.py, `agent_label`: change the fallback of the `.get` on the final line from the literal `"agent"` to the `role` argument itself (e.g. `return _AGENT_LABELS.get(role, role)`).
Why the item exists (context, not your task): `agent_label(role)` maps known roles to labels but returns the string `"agent"` for anything not in `_AGENT_LABELS`; its callers (`agents.py`, `console.py`) pass `role=agent.type`, so any custom, new, or unknown agent type (e.g. `type="researcher") is rendered as the generic "agent" rather than as `researcher`.
> `    return _AGENT_LABELS.get(role, "agent")`
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/agent_label.py`** — Changed the fallback of the `.get` on the final line of `agent_label` in src/evomesh/agent_label.py from the literal `"agent"` to the `role` argument (`return _AGENT_LABELS.get(role, role)`), so custom/unknown agent roles are returned verbatim instead of the generic "agent"; also updated the docstring to match and added a test asserting `agent_label("researcher") == "researcher"`.

   ```diff
   --- a/src/evomesh/agent_label.py
   +++ b/src/evomesh/agent_label.py
   @@ -22,3 +22,3 @@
        ``contracts.py`` documents.
        """
   -    return _AGENT_LABELS.get(role, "agent")+    return _AGENT_LABELS.get(role, role)
   ```

2. **Change to `src/evomesh/agent_label.py`** — Changed the fallback of the `.get` on the final line of `agent_label` in src/evomesh/agent_label.py from the literal `"agent"` to the `role` argument (`return _AGENT_LABELS.get(role, role)`), so custom/unknown agent roles are returned verbatim instead of the generic "agent"; also updated the docstring to match and added a test asserting `agent_label("researcher") == "researcher"`.

   ```diff
   --- a/src/evomesh/agent_label.py
   +++ b/src/evomesh/agent_label.py
   @@ -18,7 +18,7 @@
    
        Roles present in :data:`_AGENT_LABELS` are mapped to their label; any other
   -    role is treated as a plain ``agent`` rather than being returned verbatim, so
   -    every caller gets the canonical non-role label the shared contract in
   -    ``contracts.py`` documents.
   +    role is returned verbatim, so a custom or unknown agent type (e.g.
   +    ``researcher``) keeps its own name instead of being rendered as the generic
   +    ``agent``.
        """
        return _AGENT_LABELS.get(role, role)
   ```

3. **Change to `tests/test_agent_label.py`** — Changed the fallback of the `.get` on the final line of `agent_label` in src/evomesh/agent_label.py from the literal `"agent"` to the `role` argument (`return _AGENT_LABELS.get(role, role)`), so custom/unknown agent roles are returned verbatim instead of the generic "agent"; also updated the docstring to match and added a test asserting `agent_label("researcher") == "researcher"`.

   ```diff
   --- a/tests/test_agent_label.py
   +++ b/tests/test_agent_label.py
   @@ -4,2 +4,6 @@
    def test_agent_label_maps_a_known_role_to_its_short_label():
        assert agent_label("trader") == "Trader"
   +
   +
   +def test_agent_label_returns_unknown_role_verbatim():
   +    assert agent_label("researcher") == "researcher"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
