# Generation 1514

- **Opened:** 2026-09-25 01:56 UTC
- **Parent generation:** 1503
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The lock file is created in acquire() but never removed, leaving an empty `evomesh.lock` behind in `.runtime/` on every shutdown.

*Standing goal: Refill the improvement backlog from src/evomesh/singleton.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/singleton.py and write it down as one new item with its steps -- do not fix it.
What the module is for: Refuse to start a second EvoMesh against the same data.
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit
  - Log the exception detail when a watcher command times out*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — The lock file is created in acquire() but never removed, leaving an empty `evomesh.lock` behind in `.runtime/` on every shutdown.

   ```diff
   +- [ ] Delete the lock file in SingletonLock.release()
   +    The singleton lock writes the file at self._path (`.runtime/evomesh.lock` from config, created via `self._path.mkdir(parents=True, exist_ok=True)` then `self._path.write_bytes(b"\0")` in acquire()), but release() only unlocks and closes the file handle — it never unlinks the file, so the lock file lingers forever after each process exits.
   +    > `self._path.write_bytes(b"\0")`
   +    > `handle.close()`
   +    1. [ ] src/evomesh/singleton.py `SingletonLock.release` -- after unlocking and before closing, delete the lock file if it exists (e.g. `try: self._path.unlink() except FileNotFoundError: pass`), so the stale lock file does not accumulate in `.runtime/`.
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
