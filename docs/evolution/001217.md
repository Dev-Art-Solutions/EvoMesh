# Generation 1217

- **Opened:** 2026-09-21 15:14 UTC
- **Parent generation:** 1210
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I changed the `agent_label()` fallback in `src/evomesh/agent_label.py` from returning the role string verbatim to returning the canonical "agent" label, plus a matching docstring update, so the shared label utility agrees with the `contracts.py` contract that documents "agent" as the standard non-role label.

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/agent_label.py`** — I changed the `agent_label()` fallback in `src/evomesh/agent_label.py` from returning the role string verbatim to returning the canonical "agent" label, plus a matching docstring update, so the shared label utility agrees with the `contracts.py` contract that documents "agent" as the standard non-role label.

   ```diff
   --- a/src/evomesh/agent_label.py
   +++ b/src/evomesh/agent_label.py
   @@ -17,6 +17,8 @@
        """Return a short human label for the given agent role.
    
   -    Unknown roles fall back to the role string unchanged so callers can
   -    rely on a non-empty, always-valid label.
   +    Roles present in :data:`_AGENT_LABELS` are mapped to their label; any other
   +    role is treated as a plain ``agent`` rather than being returned verbatim, so
   +    every caller gets the canonical non-role label the shared contract in
   +    ``contracts.py`` documents.
        """
   -    return _AGENT_LABELS.get(role, role)+    return _AGENT_LABELS.get(role, "agent")
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
