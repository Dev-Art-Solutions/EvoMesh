# Generation 1383

- **Opened:** 2026-09-24 11:01 UTC
- **Parent generation:** 1382
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** In `GenerationSupervisor.discard`, appended the outcome `discarded` to the capped `recent_outcomes` list in supervisor metadata (last 20), mirroring what `promote()` writes for `promoted`, so the later `/evolution status` line can summarize the last ~20 outcomes.

*Standing goal: Implement this improvement to EvoMesh: Show the Evolver's recent success rate in `/evolution status` [step 2] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/evolution.py, `GenerationSupervisor.discard`: append the outcome `discarded` to that same last-20 list in the supervisor metadata, alongside what `promote` writes.
Why the item exists (context, not your task): Nothing tells a human whether evolution is actually working: 363 of the last ~550
generations were discarded "not validated" and that was only found by grepping mesh.log.
Nothing keeps that count today: `GenerationSupervisor.promote` and
`GenerationSupervisor.discard` (src/evomesh/evolution.py) both drop the candidate's entry
from supervisor.json and remember nothing about how it ended. Have both also append the
outcome to a short list in the metadata (the last 20), and add one line built from it to
the `/evolution status` output (`ConsoleChannel._command_evolution` in
src/evomesh/console.py), e.g. `recent: 4 promoted, 16 discarded of the last 20`.
Already landed: 1. src/evomesh/evolution.py `GenerationSupervisor.promote` -- append the outcome `promoted` to a capped list (keep the last 20) stored in the supervisor metadata, creating the key on first use.
Later steps, NOT this one: 3. src/evomesh/console.py `ConsoleChannel._command_evolution` -- add one line summarising the last ~20 outcomes (e.g. `recent: 4 promoted, 16 discarded of the last 20`), built from the list `promote` and `discard` maintain.
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/evolution.py`** — In `GenerationSupervisor.discard`, appended the outcome `discarded` to the capped `recent_outcomes` list in supervisor metadata (last 20), mirroring what `promote()` writes for `promoted`, so the later `/evolution status` line can summarize the last ~20 outcomes.

   ```diff
   --- a/src/evomesh/evolution.py
   +++ b/src/evomesh/evolution.py
   @@ -776,4 +776,7 @@
                raise ValueError(f"Generation {number} is not a known candidate")
            metadata["candidates"] = candidates
   +        history = list(metadata.get("recent_outcomes", []))
   +        history.append("discarded")
   +        metadata["recent_outcomes"] = history[-20:]
            self._write(metadata)
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
