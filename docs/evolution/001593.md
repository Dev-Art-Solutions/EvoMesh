# Generation 1593

- **Opened:** 2026-09-25 09:37 UTC
- **Parent generation:** 1588
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `parameters_schema` must return the real, validated type of each parameter; making `parameters_schema` surface `p.type` also means an invalid type (like a typo) has to be caught and validated at parse time via the new validator, since `parameters_schema` is the public contract that consumers rely on to build `pydantic` validators — today that value is unvalidated and silently wrong.

*Standing goal: Refill the improvement backlog from src/evomesh/tools.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/tools.py and write it down as one new item with its steps -- do not fix it.
What the module is for: A custom tool: a name and a description over one allow-listed command.
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Set the watcher's command timeout from settings instead of the hardcoded 20s default
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit
  - Log the exception detail when a watcher command times out
  - Delete the lock file in SingletonLock.release()
  - Give `ToolDefinition.parameters_schema` the real JSON schema type of each parameter instead of always "string"
  - Make `agent_label` return the role itself for roles absent from `_AGENT_LABELS` instead of the hard-coded "agent"
  - Capitalize only the first letter when rendering an unknown phase, not the whole string
  - Add backoff so a watcher that keeps timing out slows down instead of hammering the failing command every interval
  - Make `_sum` use a correctly-rounded float sum instead of a running total
  - Fix `phase_label` to return `None` for an empty/whitespace/`None` phase, matching its documented contract ("empty or `None` -> `None`")*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — `parameters_schema` must return the real, validated type of each parameter; making `parameters_schema` surface `p.type` also means an invalid type (like a typo) has to be caught and validated at parse time via the new validator, since `parameters_schema` is the public contract that consumers rely on to build `pydantic` validators — today that value is unvalidated and silently wrong.

   ```diff
   +- [ ] Validate a tool parameter's declared `type` when parsing the tool, rejecting unrecognized ones
   +    `parse_tool` reads each parameter's `type` from the tool's YAML and stores it on the `ToolParameter` model, but `ToolParameter` has no validation on it, so a malformed value (a typo like `striiing`, or `None`) is accepted silently and only surfaces later as an opaque pydantic error at runtime in `harness_tools.validator.validate_python`.
   +    > `parameters = [ToolParameter.model_validate(item) for item in raw_parameters]`
   +    1. [ ] src/evomesh/tools.py `ToolParameter` -- after the existing fields, add a Pydantic validator that raises `TypeError("... 'type' is not a recognized Python type: ...")` when `type` isn't the name of a class that Pydantic can validate against (e.g. a value of `type='striiing'` or `type=None` is rejected at parse time).
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
