# Generation 1415

- **Opened:** 2026-09-24 15:25 UTC
- **Parent generation:** 1414
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The function is a pure string-builder that joins `project`, the node path, `title`, `reasoning`, and rules. The single obvious check is that the inputs it was handed appear in the output — the project string and the "ITEM TITLE:" line — rather than testing every branch.

*Standing goal: Write ONE small, mechanical test for `decompose_objective()` in `src/evomesh/evolution.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `decompose_objective` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.*

### What it changed, and the reason it gave

1. **Change to `tests/test_evolution.py`** — The function is a pure string-builder that joins `project`, the node path, `title`, `reasoning`, and rules. The single obvious check is that the inputs it was handed appear in the output — the project string and the "ITEM TITLE:" line — rather than testing every branch.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -9,5 +9,12 @@
    from __future__ import annotations
    
   -from evomesh.evolution import clip, harness_objective, parse_plan_verdict, review_objective
   +from evomesh.evolution import (
   +    PlanNode,
   +    clip,
   +    decompose_objective,
   +    harness_objective,
   +    parse_plan_verdict,
   +    review_objective,
   +)
   ```

2. **Change to `tests/test_evolution.py`** — The function is a pure string-builder that joins `project`, the node path, `title`, `reasoning`, and rules. The single obvious check is that the inputs it was handed appear in the output — the project string and the "ITEM TITLE:" line — rather than testing every branch.

   ```diff
   --- a/tests/test_evolution.py
   +++ b/tests/test_evolution.py
   @@ -38,2 +38,9 @@
    def test_clip_keeps_the_tail_when_longer_than_limit() -> None:
        assert clip("abcdef", 2) == "...\nef"
   +
   +
   +def test_decompose_objective_embeds_project_and_title() -> None:
   +    node = PlanNode(id="leafA", title="item A", reasoning="change alpha")
   +    prompt = decompose_objective(node, "evomesh")
   +    assert "evomesh" in prompt
   +    assert "ITEM TITLE: item A" in prompt
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
