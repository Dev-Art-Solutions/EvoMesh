# Generation 1409

- **Opened:** 2026-09-24 15:02 UTC
- **Parent generation:** 1405
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `without_virtual_env()` takes no parameters, so the simplest real call is with none. The code's one guaranteed behavior is that it returns a copy of `os.environ` with the `VIRTUAL_ENV` key dropped, so the assertion checks both that the key is absent and that the result matches a direct list-comprehension rebuild of `os.environ` without that key — a single, real, passing check. The test is a plain sync function (matching the target's nature) and added no mocks/stubs; `os` and `without_virtual_env` were imported, and only `tests/` was changed.

*Standing goal: Write ONE small, mechanical test for `without_virtual_env()` in `src/evomesh/processes.py`. It is exported and load-bearing (used by 5 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `without_virtual_env` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: Run an external command without leaving an asyncio transport behind.*

### What it changed, and the reason it gave

1. **Change to `tests/test_processes.py`** — `without_virtual_env()` takes no parameters, so the simplest real call is with none. The code's one guaranteed behavior is that it returns a copy of `os.environ` with the `VIRTUAL_ENV` key dropped, so the assertion checks both that the key is absent and that the result matches a direct list-comprehension rebuild of `os.environ` without that key — a single, real, passing check. The test is a plain sync function (matching the target's nature) and added no mocks/stubs; `os` and `without_virtual_env` were imported, and only `tests/` was changed.

   ```diff
   --- a/tests/test_processes.py
   +++ b/tests/test_processes.py
   @@ -15,4 +15,5 @@
    
    from evomesh.processes import run_command
   +from evomesh.processes import without_virtual_env
   ```

2. **Change to `tests/test_processes.py`** — `without_virtual_env()` takes no parameters, so the simplest real call is with none. The code's one guaranteed behavior is that it returns a copy of `os.environ` with the `VIRTUAL_ENV` key dropped, so the assertion checks both that the key is absent and that the result matches a direct list-comprehension rebuild of `os.environ` without that key — a single, real, passing check. The test is a plain sync function (matching the target's nature) and added no mocks/stubs; `os` and `without_virtual_env` were imported, and only `tests/` was changed.

   ```diff
   --- a/tests/test_processes.py
   +++ b/tests/test_processes.py
   @@ -36,2 +36,9 @@
        assert result.exit_code == 0
        assert "hi" in result.output
   +
   +
   +def test_without_virtual_env_returns_the_environment_without_VIRTUAL_ENV() -> None:
   +    result = without_virtual_env()
   +
   +    assert "VIRTUAL_ENV" not in result
   +    assert result == {k: v for k, v in os.environ.items() if k != "VIRTUAL_ENV"}
   ```

3. **Change to `tests/test_processes.py`** — `without_virtual_env()` takes no parameters, so the simplest real call is with none. The code's one guaranteed behavior is that it returns a copy of `os.environ` with the `VIRTUAL_ENV` key dropped, so the assertion checks both that the key is absent and that the result matches a direct list-comprehension rebuild of `os.environ` without that key — a single, real, passing check. The test is a plain sync function (matching the target's nature) and added no mocks/stubs; `os` and `without_virtual_env` were imported, and only `tests/` was changed.

   ```diff
   --- a/tests/test_processes.py
   +++ b/tests/test_processes.py
   @@ -12,4 +12,5 @@
    from __future__ import annotations
    
   +import os
    import time
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
