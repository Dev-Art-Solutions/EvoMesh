# Generation 271

- **Opened:** 2026-09-09 03:46 UTC
- **Parent generation:** 261
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## How it was planned

- **Plan draft** — approved

  > ...
  > roving the dead module is reachable and
  > correct before it is depended on. Item 2 is the heart of the job and is the
  > smallest change that does the job — it only touches id generation in one loop, and
  > `AgentId` being a `str` subclass means nothing downstream has to change shape.
  > Item 3 reuses the same service for validation, so it is cheap and reuses the test
  > infrastructure from Item 2. Item 4 is the only item that crosses a second module
  > boundary (`harness_session.py`), so it is last and explicitly optional — the plan
  > ends at a verifiable, complete state (Items 1-3) regardless of whether Item 4
  > runs.
  > 
  > ## Verification
  > - Every item has an added test (smoke, stability, validation) run before the next
  >   item starts.
  > - No live module signature changes in Items 1-3 (only id generation and validation
  >   swap).
  > - The dead module `_agent_ids.py` is now imported from live code and its code runs.
  > - Final state: ids are generated in one place (`AgentIdSequence`), registered in
  >   one place (`AgentIdRegistry`), and validated in one place (`AgentIdValidator`),
  >   with an agent carrying a single stable id from roster build through the job.
  > 
  > RATIONALE: Wire the dead `_agent_ids.py` service (AgentId value object, AgentIdSequence, AgentIdRegistry, AgentIdValidator) into the roster-id generation and harness validation path in environment.py/harness.py so agent ids are generated, registered, and validated in one stable place, done as small verifiable items that don't change any downstream signature.

  - **Expose the dead `_agent_ids.py` service and record a smoke-test baseline** (leaf) — Prove the asset exists and is importable from live code, and add a smoke test that constructs `AgentId`, `AgentIdSequence`, `AgentIdRegistry`, and `AgentIdValid...
  - **Generate roster ids through the `AgentIdSequence` and register them in an `AgentIdRegistry` in `environment.py`** (leaf) — In the roster-building loop, replace the ad-hoc `str(uuid4())` with one scoped `AgentIdSequence` (`next_id()` per agent), register each returned id in an `Agent...
  - **Validate agent ids at the harness boundary with `AgentIdValidator` in `harness_tools.py`** (leaf) — Replace the harness's hand-rolled id checks (or their absence) with `AgentIdValidator.is_valid(...)`, keeping the accept-valid/reject-malformed semantics identi...
  - **Make session ids stable by reusing the roster id from the registry in `harness_session.py`** (leaf) — Have the session reuse the roster id (from the registry) instead of generating a fresh one, so an agent carries one id through the whole job; optional and secon...

## Why this change

**What it set out to do.** # Plan: Introduce a stable AgentId service ## Goal Give EvoMesh a single, shared, stable identity service for agents instead of the current ad-hoc, per-site id assignment. Today an agent's `id` is a `str(uuid4())` generated at different times in different places (`environment.py` at roster build, `harness_session.py` at session start), and the one library that actually implements agent-id concepts...

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/_agent_ids.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/_agent_ids.py
   +++ b/src/evomesh/_agent_ids.py
   @@ -81,3 +81,78 @@
        global _next_seq
        _next_seq += 1
   -    return f"{_next_seq:0{width}d}"+    return f"{_next_seq:0{width}d}"
   +
   +
   +class AgentIdSequence:
   +    """A private, monotonic generator of well-formed agent ids.
   +
   +    Wraps the module-level ``next_id`` counter so a single owner can hand out
   +    ids without sharing state, while still validating that every id it produces
   +    is well-formed.  ``next_id`` returns the same string ``make_id`` would for
   +    the same value, so ids are comparable and importable.
   +    """
   +
   +    def __init__(self, width: int = 4) -> None:
   +        self._width = width
   +
   +    def next_id(self) -> str:
   +        """Return the next well-formed id from this sequence (see ``make_id``)."""
   +        return next_id(self._width)
   +
   +    def __iter__(self):
   +        return self
   +
   +    def __next__(self) -> str:
   +        return self.next_id()
   +
   +
   +class AgentIdRegistry:
   +    """The set of ids the mesh currently knows about.
   +
   +    Backed by a plain ``set`` of strings so lookups are O(1); every id stored
   +    here is validated on the way in, so ``contains`` can never report a
   +    malformed 
   ...
   ```

2. **Change to `src/evomesh/agents.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/agents.py
   +++ b/src/evomesh/agents.py
   @@ -10,4 +10,5 @@
    
    from evomesh import cron
   +from evomesh._agent_ids import AgentIdSequence
    from evomesh.bdi import ReflectiveBehavior
    from evomesh.cognition import AgentBehavior, CycleContext, CycleOutcome
   ```

3. **Change to `src/evomesh/environment.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/environment.py
   +++ b/src/evomesh/environment.py
   @@ -14,4 +14,5 @@
    from evomesh.behaviors import default_behaviors
    from evomesh.cognition import AgentBehavior, CycleOutcome
   +from evomesh._agent_ids import AgentIdRegistry, AgentIdSequence, make_id
    from evomesh.config import Settings
    from evomesh.contracts import (
   ```

4. **Change to `src/evomesh/environment.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/environment.py
   +++ b/src/evomesh/environment.py
   @@ -10,9 +10,9 @@
    from typing import Any
    
   +from evomesh._agent_ids import AgentIdRegistry, AgentIdSequence, make_id
    from evomesh.agents import AgentRegistry, AgentRuntime, system_agent_definitions
    from evomesh.bdi import ReflectiveBehavior
    from evomesh.behaviors import default_behaviors
    from evomesh.cognition import AgentBehavior, CycleOutcome
   -from evomesh._agent_ids import AgentIdRegistry, AgentIdSequence, make_id
    from evomesh.config import Settings
    from evomesh.contracts import (
   ```

5. **Change to `src/evomesh/harness_tools.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/harness_tools.py
   +++ b/src/evomesh/harness_tools.py
   @@ -24,4 +24,5 @@
    from typing import Any
    
   +from evomesh._agent_ids import AgentIdValidator
    from evomesh.harness_session import HarnessSession
    from evomesh.permissions import FilesystemPolicy, PermissionDeniedError
   ```

6. **Change to `src/evomesh/harness_tools.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/harness_tools.py
   +++ b/src/evomesh/harness_tools.py
   @@ -138,4 +138,17 @@
            raise ToolDenied(f"DENIED: {raw} is outside the job root {root}")
        return target
   +
   +
   +def valid_id(agent_id: str) -> bool:
   +    """Whether ``agent_id`` is one the harness will answer about.
   +
   +    The harness only ever touches a fixed, well-formed id space. Anything that
   +    breaks the ``<namespace>:<name>`` shape -- or, for namespaces, the
   +    ``<namespace>/<name>`` shape -- is rejected out of hand, so a malformed id
   +    from a model never reaches the file policy. The shape check itself lives in
   +    :class:`evomesh._agent_ids.AgentIdValidator`; this function is the harness's
   +    view of that single source of truth.
   +    """
   +    return AgentIdValidator.is_valid(agent_id)
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
