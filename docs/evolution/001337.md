# Generation 1337

- **Opened:** 2026-09-23 22:42 UTC
- **Parent generation:** 1336
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I read the real implementation to confirm it returns True for a 400 whose body contains "tool", then added a single passing test plus the import to the existing `tests/test_models.py`, reusing the file's already-imported `httpx` and mirroring its existing error-construction style.

*Standing goal: Write ONE small, mechanical test for `_tools_are_unsupported()` in `src/evomesh/models.py`. It is exported and load-bearing (used by 9 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Read the real definition first -- do not guess its signature or behavior from the name alone. Call `_tools_are_unsupported` with the simplest realistic arguments and assert the one obvious thing about its result. Do not try to cover every branch or every edge case -- one real, passing check that exercises actual behavior is a complete answer.
Do not invent a mock or stub class from scratch. If this needs a stand-in for a dependency, search tests/ first for one that already exists and reuse it -- a test that references something imagined is worse than no test at all.
Add it to the existing test file for this module if one exists (tests/test_models.py), or create one if it does not.*

### What it changed, and the reason it gave

1. **Change to `tests/test_models.py`** — I read the real implementation to confirm it returns True for a 400 whose body contains "tool", then added a single passing test plus the import to the existing `tests/test_models.py`, reusing the file's already-imported `httpx` and mirroring its existing error-construction style.

   ```diff
   --- a/tests/test_models.py
   +++ b/tests/test_models.py
   @@ -6,4 +6,5 @@
        _parse_arguments,
        _post_with_retry,
   +    _tools_are_unsupported,
    )
   ```

2. **Change to `tests/test_models.py`** — I read the real implementation to confirm it returns True for a 400 whose body contains "tool", then added a single passing test plus the import to the existing `tests/test_models.py`, reusing the file's already-imported `httpx` and mirroring its existing error-construction style.

   ```diff
   --- a/tests/test_models.py
   +++ b/tests/test_models.py
   @@ -51,2 +51,9 @@
    
        assert out == "thinking step by step"
   +
   +
   +def test_tools_are_unsupported_flags_a_400_mentioning_tool():
   +    response = httpx.Response(400, text="This model does not support tools")
   +    exc = httpx.HTTPStatusError("boom", request=response.request, response=response)
   +
   +    assert _tools_are_unsupported(exc) is True
   ```

3. **Repair to `tests/test_models.py`** — The test constructed `httpx.HTTPStatusError("boom", request=response.request, ...)` where `response` was a bare `httpx.Response` with no request set, so `response.request` raised `RuntimeError`; I created a real `httpx.Request`, attached it to the response and error, and the test now passes (production code only reads `exc.response`).

   ```diff
   --- a/tests/test_models.py
   +++ b/tests/test_models.py
   @@ -54,6 +54,7 @@
    
    def test_tools_are_unsupported_flags_a_400_mentioning_tool():
   -    response = httpx.Response(400, text="This model does not support tools")
   -    exc = httpx.HTTPStatusError("boom", request=response.request, response=response)
   +    request = httpx.Request("POST", "https://example.com")
   +    response = httpx.Response(400, text="This model does not support tools", request=request)
   +    exc = httpx.HTTPStatusError("boom", request=request, response=response)
    
        assert _tools_are_unsupported(exc) is True
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
