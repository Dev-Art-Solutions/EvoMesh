# Generation 1566

- **Opened:** 2026-09-25 06:50 UTC
- **Parent generation:** 1550
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `_loop` currently re-runs a command on a fixed short interval even when that command keeps timing out, which the running mesh reproduced three times in a row (`3x Watcher command timed out`); since each timeout forces `run_command` to kill the whole process group, launching the next copy just `interval_seconds` later wastes cycles on a command that hasn't had time to succeed. Backing off on consecutive timeouts (as done for Telegram polling, per the "Back off exponentially when Telegram polling keeps failing" item) spaces out the failing re-launches, and the existing `except Exception: ... self._timeout_streak = 0` reset path already exists to clear the counter when a non-timeout failure occurs, so no other function needs to change.

*Standing goal: Refill the improvement backlog from src/evomesh/watchers.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/watchers.py and write it down as one new item with its steps -- do not fix it.
What the module is for: A deterministic watcher: a command run on its own short, fixed interval,
What the running mesh logged from it since the file last changed (numbers masked) -- the strongest lead there is:
  - 3x Watcher command timed out: ['python', 'D:\\Projects\\Dev-art solutions\\EvoMesh\\agent-templates\\news-watcher/scripts/watch_news.py']
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Set the watcher's command timeout from settings instead of the hardcoded 20s default
  - Make cron treat weekday `7` as Sunday
  - Render week-plus durations in `humanize_duration` using the true second-counts per unit
  - Log the exception detail when a watcher command times out
  - Delete the lock file in SingletonLock.release()
  - Give `ToolDefinition.parameters_schema` the real JSON schema type of each parameter instead of always "string"
  - Make `agent_label` return the role itself for roles absent from `_AGENT_LABELS` instead of the hard-coded "agent"
  - Capitalize only the first letter when rendering an unknown phase, not the whole string

The last attempt at this exact target failed. Read this before trying again, so you do not repeat it:
it made no real edit at all -- the candidate was discarded before anything could even be validated.*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — `_loop` currently re-runs a command on a fixed short interval even when that command keeps timing out, which the running mesh reproduced three times in a row (`3x Watcher command timed out`); since each timeout forces `run_command` to kill the whole process group, launching the next copy just `interval_seconds` later wastes cycles on a command that hasn't had time to succeed. Backing off on consecutive timeouts (as done for Telegram polling, per the "Back off exponentially when Telegram polling keeps failing" item) spaces out the failing re-launches, and the existing `except Exception: ... self._timeout_streak = 0` reset path already exists to clear the counter when a non-timeout failure occurs, so no other function needs to change.

   ```diff
   +- [ ] Add backoff so a watcher that keeps timing out slows down instead of hammering the failing command every interval
   +    `AgentWatcher._loop` catches `TimeoutError`, logs one line, and then falls straight to `asyncio.sleep(self.interval_seconds)` and loops — so three consecutive timeouts (the logged failure) means three commands launched `interval_seconds` apart with no delay growing with the failure count, even though `run_command` has to tear down the whole process group after each one.
   +    >             except TimeoutError:
   +    >                 logger.warning("Watcher command timed out: %s", self._argv)
   +    >             except Exception:  # noqa: BLE001 - one bad tick must not end the watcher
   +    >                 logger.exception("Watcher command failed: %s", self._argv)
   +    >             await asyncio.sleep(self.interval_seconds)
   +    1. [ ] src/evomesh/watchers.py `AgentWatcher._loop` -- add a `self._timeout_streak` counter (set to 0 in `__init__`): in the `except TimeoutError:` branch do `self._timeout_streak = min((self._timeout_streak + 1) % 32, 31)`; in the `except Exception:` branch do `self._timeout_streak = 0`; and replace the trailing `await asyncio.sleep(
   ...
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
