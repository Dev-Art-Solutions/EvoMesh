# Generation 1362

- **Opened:** 2026-09-24 05:46 UTC
- **Parent generation:** 1361
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I appended a single passing test to the existing `tests/test_codebase.py` that calls `fabricated_references` with a realistic dotted, backtick-qualified reference string and asserts it returns `[]`, matching the function's documented scan behavior of detecting only bare or backtick references without a proper dotted qualifier.

*Standing goal: Write ONE small, mechanical test for `fabricated_references()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Read the real definition first -- do not guess its signature or behavior from the name alone. Call `fabricated_references` with the simplest realistic arguments and assert the one obvious thing about its result. Do not try to cover every branch or every edge case -- one real, passing check that exercises actual behavior is a complete answer.
Do not invent a mock or stub class from scratch. If this needs a stand-in for a dependency, search tests/ first for one that already exists and reuse it -- a test that references something imagined is worse than no test at all.
Add it to the existing test file for this module if one exists (tests/test_codebase.py), or create one if it does not.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — I appended a single passing test to the existing `tests/test_codebase.py` that calls `fabricated_references` with a realistic dotted, backtick-qualified reference string and asserts it returns `[]`, matching the function's documented scan behavior of detecting only bare or backtick references without a proper dotted qualifier.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,4 +1,5 @@
    from evomesh.codebase import (
        PACKAGE,
   +    fabricated_references,
        known_dead,
        package_root,
   ```

2. **Change to `tests/test_codebase.py`** — I appended a single passing test to the existing `tests/test_codebase.py` that calls `fabricated_references` with a realistic dotted, backtick-qualified reference string and asserts it returns `[]`, matching the function's documented scan behavior of detecting only bare or backtick references without a proper dotted qualifier.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -11,4 +11,10 @@
    def test_package_root_points_at_the_source_package(project_root):
        assert package_root(project_root) == project_root / "src" / PACKAGE
   +
   +
   +def test_fabricated_references_leaves_a_real_reference_alone(project_root):
   +    assert fabricated_references(
   +        "use `evomesh.codebase.package_root` from the package", project_root
   +    ) == []
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
