# Generation 184

- **Opened:** 2026-09-07 20:21 UTC
- **Parent generation:** 165
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The model changed `src/evomesh/contracts.py` but gave no rationale for it -- see the diff below for what actually moved.

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/contracts.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/contracts.py
   +++ b/src/evomesh/contracts.py
   @@ -12,4 +12,5 @@
    
    from evomesh import cron
   +from evomesh.agent_label import validate_label
   ```

2. **Change to `src/evomesh/contracts.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/contracts.py
   +++ b/src/evomesh/contracts.py
   @@ -21,4 +21,17 @@
    def _short_id() -> str:
        return uuid4().hex[:8]
   +
   +
   +def label(name: str) -> str:
   +    """Validate an agent label through the shared contract.
   +
   +    ``Agent.label`` is stored as a plain ``str`` and can be set directly, so the
   +    type alone cannot stop a typo like ``"engineer"`` from reaching storage.
   +    This factory runs the same ``validate_label`` check that ``agent_label``
   +    exports and returns the canonical value, so a bad label raises at the point
   +    of construction instead of being written out.
   +    """
   +
   +    return validate_label(name)
   ```

3. **Change to `src/evomesh/contracts.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/contracts.py
   +++ b/src/evomesh/contracts.py
   @@ -12,5 +12,4 @@
    
    from evomesh import cron
   -from evomesh.agent_label import validate_label
   ```

4. **Repair to `src/evomesh/contracts.py`** — The F821 error was because `contracts.label()` called a nonexistent `validate_label()` — the function in `agent_label.py` is actually named `agent_label()`, which `console.py` already imports and uses; I imported `agent_label` into `contracts.py`, pointed the call at it (both callers pass through `str()` so both string and enum inputs work), and corrected the stale docstring reference.

   ```diff
   --- a/src/evomesh/contracts.py
   +++ b/src/evomesh/contracts.py
   @@ -12,4 +12,5 @@
    
    from evomesh import cron
   +from evomesh.agent_label import agent_label
   ```

5. **Repair to `src/evomesh/contracts.py`** — The F821 error was because `contracts.label()` called a nonexistent `validate_label()` — the function in `agent_label.py` is actually named `agent_label()`, which `console.py` already imports and uses; I imported `agent_label` into `contracts.py`, pointed the call at it (both callers pass through `str()` so both string and enum inputs work), and corrected the stale docstring reference.

   ```diff
   --- a/src/evomesh/contracts.py
   +++ b/src/evomesh/contracts.py
   @@ -33,5 +33,5 @@
        """
    
   -    return validate_label(name)
   +    return agent_label(name)
   ```

6. **Repair to `src/evomesh/contracts.py`** — The F821 error was because `contracts.label()` called a nonexistent `validate_label()` — the function in `agent_label.py` is actually named `agent_label()`, which `console.py` already imports and uses; I imported `agent_label` into `contracts.py`, pointed the call at it (both callers pass through `str()` so both string and enum inputs work), and corrected the stale docstring reference.

   ```diff
   --- a/src/evomesh/contracts.py
   +++ b/src/evomesh/contracts.py
   @@ -28,5 +28,5 @@
        ``Agent.label`` is stored as a plain ``str`` and can be set directly, so the
        type alone cannot stop a typo like ``"engineer"`` from reaching storage.
   -    This factory runs the same ``validate_label`` check that ``agent_label``
   +    This factory runs the same ``agent_label`` check that the shared contract
        exports and returns the canonical value, so a bad label raises at the point
        of construction instead of being written out.
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
