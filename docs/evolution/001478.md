# Generation 1478

- **Opened:** 2026-09-24 21:01 UTC
- **Parent generation:** 1477
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `SkillDefinition` is exported and used by 17 modules but has no direct coverage; a single constructor + two trivial round-trip assertions on its real fields gives it minimal, honest coverage that matches the existing test's style.

*Standing goal: Write ONE small, mechanical test for `SkillDefinition` in `src/evomesh/contracts.py`. It is exported and load-bearing (used by 17 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Construct one `SkillDefinition` the way an existing test constructs something similar, call one real method on it, and assert one obvious thing about the result.*

### What it changed, and the reason it gave

1. **Change to `tests/test_contracts.py`** — `SkillDefinition` is exported and used by 17 modules but has no direct coverage; a single constructor + two trivial round-trip assertions on its real fields gives it minimal, honest coverage that matches the existing test's style.

   ```diff
   --- a/tests/test_contracts.py
   +++ b/tests/test_contracts.py
   @@ -1,5 +1,5 @@
    """Coverage for the small, load-bearing pieces of evomesh.contracts."""
    
   -from evomesh.contracts import AgentRuntimeState, belief_key
   +from evomesh.contracts import AgentRuntimeState, belief_key, SkillDefinition
   ```

2. **Change to `tests/test_contracts.py`** — `SkillDefinition` is exported and used by 17 modules but has no direct coverage; a single constructor + two trivial round-trip assertions on its real fields gives it minimal, honest coverage that matches the existing test's style.

   ```diff
   --- a/tests/test_contracts.py
   +++ b/tests/test_contracts.py
   @@ -11,2 +11,8 @@
        state = AgentRuntimeState(agent_id="agent-1", goal="find water")
        assert "goal=find water" in state.describe()
   +
   +
   +def test_skill_definition_is_built_from_its_fields() -> None:
   +    skill = SkillDefinition(name="read_file", description="Read a file", path=Path("skills/read_file/SKILL.md"))
   +    assert skill.name == "read_file"
   +    assert skill.path == Path("skills/read_file/SKILL.md")
   ```

3. **Change to `tests/test_contracts.py`** — `SkillDefinition` is exported and used by 17 modules but has no direct coverage; a single constructor + two trivial round-trip assertions on its real fields gives it minimal, honest coverage that matches the existing test's style.

   ```diff
   --- a/tests/test_contracts.py
   +++ b/tests/test_contracts.py
   @@ -1,3 +1,5 @@
    """Coverage for the small, load-bearing pieces of evomesh.contracts."""
   +
   +from pathlib import Path
    
    from evomesh.contracts import AgentRuntimeState, belief_key, SkillDefinition
   ```

4. **Repair to `tests/test_contracts.py`** — The single ruff failure was a 112-character line exceeding the 100 limit; wrapping the constructor call across lines fixes it without altering any behavior or touching src/evomesh/.

   ```diff
   --- a/tests/test_contracts.py
   +++ b/tests/test_contracts.py
   @@ -16,5 +16,9 @@
    
    def test_skill_definition_is_built_from_its_fields() -> None:
   -    skill = SkillDefinition(name="read_file", description="Read a file", path=Path("skills/read_file/SKILL.md"))
   +    skill = SkillDefinition(
   +        name="read_file",
   +        description="Read a file",
   +        path=Path("skills/read_file/SKILL.md"),
   +    )
        assert skill.name == "read_file"
        assert skill.path == Path("skills/read_file/SKILL.md")
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
