# Generation 1470

- **Opened:** 2026-09-24 20:28 UTC
- **Parent generation:** 1468
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The test builds the Pydantic v2 `ScrapingSettings` the same way `TelegramSettings` is built in an existing test, calls the real `model_dump()` method, and asserts the dict equals the four constructed field values — the one obvious behaviour the code under test exhibits — without importing any project code that `shell` can't reach or touching anything under `src/`.

*Standing goal: Write ONE small, mechanical test for `ScrapingSettings` in `src/evomesh/config.py`. It is exported and load-bearing (used by 3 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Construct one `ScrapingSettings` the way an existing test constructs something similar, call one real method on it, and assert one obvious thing about the result.*

### What it changed, and the reason it gave

1. **Change to `tests/test_config.py`** — The test builds the Pydantic v2 `ScrapingSettings` the same way `TelegramSettings` is built in an existing test, calls the real `model_dump()` method, and asserts the dict equals the four constructed field values — the one obvious behaviour the code under test exhibits — without importing any project code that `shell` can't reach or touching anything under `src/`.

   ```diff
   --- a/tests/test_config.py
   +++ b/tests/test_config.py
   @@ -14,5 +14,5 @@
    import pytest
    
   -from evomesh.config import HarnessSettings, RuntimeSettings, load_settings
   +from evomesh.config import HarnessSettings, RuntimeSettings, ScrapingSettings, load_settings
   ```

2. **Change to `tests/test_config.py`** — The test builds the Pydantic v2 `ScrapingSettings` the same way `TelegramSettings` is built in an existing test, calls the real `model_dump()` method, and asserts the dict equals the four constructed field values — the one obvious behaviour the code under test exhibits — without importing any project code that `shell` can't reach or touching anything under `src/`.

   ```diff
   --- a/tests/test_config.py
   +++ b/tests/test_config.py
   @@ -217,2 +217,16 @@
    
        assert settings.models.providers["openai"].api_key == "sk-literal"
   +def test_scraping_settings_dump_the_values_it_is_constructed_with() -> None:
   +    scraping = ScrapingSettings(
   +        enabled=True,
   +        executable="/usr/local/bin/scrapling",
   +        timeout_seconds=45.0,
   +        max_content_chars=15000,
   +    )
   +
   +    assert scraping.model_dump() == {
   +        "enabled": True,
   +        "executable": "/usr/local/bin/scrapling",
   +        "timeout_seconds": 45.0,
   +        "max_content_chars": 15000,
   +    }
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
