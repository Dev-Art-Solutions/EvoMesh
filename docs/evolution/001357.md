# Generation 1357

- **Opened:** 2026-09-24 03:55 UTC
- **Parent generation:** 1356
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I read the real `survey(root)` definition to confirm it returns `list[Module]` (each with `.name` and `.path`), then added one focused test to the existing `tests/test_codebase.py` that calls `survey(project_root)` with the real repo root and asserts the `codebase` module appears and its `.path` points at the real `src/evomesh/codebase.py` file.

*Standing goal: Write ONE small, mechanical test for `survey()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Read the real definition first -- do not guess its signature or behavior from the name alone. Call `survey` with the simplest realistic arguments and assert the one obvious thing about its result. Do not try to cover every branch or every edge case -- one real, passing check that exercises actual behavior is a complete answer.
Do not invent a mock or stub class from scratch. If this needs a stand-in for a dependency, search tests/ first for one that already exists and reuse it -- a test that references something imagined is worse than no test at all.
Add it to the existing test file for this module if one exists (tests/test_codebase.py), or create one if it does not.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — I read the real `survey(root)` definition to confirm it returns `list[Module]` (each with `.name` and `.path`), then added one focused test to the existing `tests/test_codebase.py` that calls `survey(project_root)` with the real repo root and asserts the `codebase` module appears and its `.path` points at the real `src/evomesh/codebase.py` file.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,5 +1,13 @@
   -from evomesh.codebase import PACKAGE, package_root
   +from evomesh.codebase import PACKAGE, Module, package_root
    
    
    def test_package_root_points_at_the_source_package(project_root):
        assert package_root(project_root) == project_root / "src" / PACKAGE
   +
   +
   +def test_survey_reports_the_load_bearing_modules(project_root):
   +    modules = {module.name for module in survey(project_root)}
   +    assert "codebase" in modules
   +
   +    base = next(module for module in survey(project_root) if module.name == "codebase")
   +    assert base.path == package_root(project_root) / "codebase.py"
   ```

2. **Change to `tests/test_codebase.py`** — I read the real `survey(root)` definition to confirm it returns `list[Module]` (each with `.name` and `.path`), then added one focused test to the existing `tests/test_codebase.py` that calls `survey(project_root)` with the real repo root and asserts the `codebase` module appears and its `.path` points at the real `src/evomesh/codebase.py` file.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,3 +1,3 @@
   -from evomesh.codebase import PACKAGE, Module, package_root
   +from evomesh.codebase import PACKAGE, Module, package_root, survey
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
