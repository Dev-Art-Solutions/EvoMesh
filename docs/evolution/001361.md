# Generation 1361

- **Opened:** 2026-09-24 05:35 UTC
- **Parent generation:** 1359
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** I added a single passing test to the existing `tests/test_codebase.py` that calls `stray_root_files(tmp_path)` on a throwaway temp directory containing one orphaned file and asserts the returned list is `["orphan.py"]` — exercising the real behavior (a file not in KNOWN_ROOT_FILES is reported, sorted) while matching the module's existing pytest style.

*Standing goal: Write ONE small, mechanical test for `stray_root_files()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Read the real definition first -- do not guess its signature or behavior from the name alone. Call `stray_root_files` with the simplest realistic arguments and assert the one obvious thing about its result. Do not try to cover every branch or every edge case -- one real, passing check that exercises actual behavior is a complete answer.
Do not invent a mock or stub class from scratch. If this needs a stand-in for a dependency, search tests/ first for one that already exists and reuse it -- a test that references something imagined is worse than no test at all.
Add it to the existing test file for this module if one exists (tests/test_codebase.py), or create one if it does not.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — I added a single passing test to the existing `tests/test_codebase.py` that calls `stray_root_files(tmp_path)` on a throwaway temp directory containing one orphaned file and asserts the returned list is `["orphan.py"]` — exercising the real behavior (a file not in KNOWN_ROOT_FILES is reported, sorted) while matching the module's existing pytest style.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,3 +1,9 @@
   -from evomesh.codebase import PACKAGE, known_dead, package_root, survey
   +from evomesh.codebase import (
   +    PACKAGE,
   +    known_dead,
   +    package_root,
   +    stray_root_files,
   +    survey,
   +)
   ```

2. **Change to `tests/test_codebase.py`** — I added a single passing test to the existing `tests/test_codebase.py` that calls `stray_root_files(tmp_path)` on a throwaway temp directory containing one orphaned file and asserts the returned list is `["orphan.py"]` — exercising the real behavior (a file not in KNOWN_ROOT_FILES is reported, sorted) while matching the module's existing pytest style.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -29,2 +29,7 @@
        baseline.write_text("# stale orphans\nreachability\n\n\norchestration.py\n")
        assert known_dead(tmp_path) == frozenset({"reachability", "orchestration.py"})
   +
   +
   +def test_stray_root_files_lists_a_file_left_at_the_project_root(tmp_path):
   +    (tmp_path / "orphan.py").write_text("# left behind by accident\n")
   +    assert stray_root_files(tmp_path) == ["orphan.py"]
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
