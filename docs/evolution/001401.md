# Generation 1401

- **Opened:** 2026-09-24 14:14 UTC
- **Parent generation:** 1397
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** Added `plausible_name` to the imports and appended a single test asserting `plausible_name("Trade") is True`, the simplest realistic input matching the docstring's contract that the code clearly returns True for.

*Standing goal: Write ONE small, mechanical test for `plausible_name()` in `src/evomesh/architect.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `plausible_name` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: Agent Architect: turn one sentence into a complete agent definition.*

### What it changed, and the reason it gave

1. **Change to `tests/test_architect.py`** — Added `plausible_name` to the imports and appended a single test asserting `plausible_name("Trade") is True`, the simplest realistic input matching the docstring's contract that the code clearly returns True for.

   ```diff
   --- a/tests/test_architect.py
   +++ b/tests/test_architect.py
   @@ -1,3 +1,3 @@
   -from evomesh.architect import derive_access, derive_model, plausible_constraints
   +from evomesh.architect import derive_access, derive_model, plausible_constraints, plausible_name
   ```

2. **Change to `tests/test_architect.py`** — Added `plausible_name` to the imports and appended a single test asserting `plausible_name("Trade") is True`, the simplest realistic input matching the docstring's contract that the code clearly returns True for.

   ```diff
   --- a/tests/test_architect.py
   +++ b/tests/test_architect.py
   @@ -23,2 +23,8 @@
        # Two non-empty fields separated by ';' -> a genuine constraints block.
        assert plausible_constraints("No trading on Friday; no trading on Saturdays") is True
   +
   +
   +def test_plausible_name_accepts_a_single_real_word_name():
   +    # A short, alphabetic, single-word name has no path/sentence/empty issues,
   +    # so plausible_name treats it as a genuine name.
   +    assert plausible_name("Trade") is True
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
