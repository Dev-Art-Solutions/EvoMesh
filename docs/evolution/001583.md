# Generation 1583

- **Opened:** 2026-09-25 08:24 UTC
- **Parent generation:** 1582
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** In `src/evomesh/metrics.py` I added `import math` and replaced `_sum`'s running-total loop with `return math.fsum(values)`, so the sum is correctly rounded (no accumulated rounding error that propagates into `mean`). I also added a regression test in `tests/test_metrics.py` asserting `_sum([1e16, 1.0, 1.0]) == 1e16 + 2.0`, which a naive running total gets wrong but `math.fsum` gets right.

*Standing goal: Implement this improvement to EvoMesh: Make `_sum` use a correctly-rounded float sum instead of a running total [step 1] -- one step of a larger item; do this step and nothing else.
THIS STEP: in src/evomesh/metrics.py, `_sum`: replace the manual `total += value` loop with `math.fsum(values)` (adding `import math` at the top) so the total — and therefore `mean` — is correctly rounded rather than accumulating rounding error.
Why the item exists (context, not your task): `_sum` accumulates values with `total += value`, so values of very different magnitudes lose precision to rounding error, which then propagates into `mean` (called on every elapsed record in `harness_session.py`). It is the sole summation primitive in the module and has no more accurate implementation; the existing test only checks `1.0+2.0+3.0 == 6.0`, which a correct sum also satisfies, so the inaccuracy is currently uncaught.
>
>     total = 0.0
>     for value in values:
>         total += value
>     return total
Do not edit docs/evolution/improvements.md: the pipeline ticks this step off once your change lands.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/metrics.py`** — In `src/evomesh/metrics.py` I added `import math` and replaced `_sum`'s running-total loop with `return math.fsum(values)`, so the sum is correctly rounded (no accumulated rounding error that propagates into `mean`). I also added a regression test in `tests/test_metrics.py` asserting `_sum([1e16, 1.0, 1.0]) == 1e16 + 2.0`, which a naive running total gets wrong but `math.fsum` gets right.

   ```diff
   --- a/src/evomesh/metrics.py
   +++ b/src/evomesh/metrics.py
   @@ -1,4 +1,5 @@
    """Lightweight metrics for evaluating EvoMesh outputs."""
    
   +import math
    from collections.abc import Iterable
   ```

2. **Change to `src/evomesh/metrics.py`** — In `src/evomesh/metrics.py` I added `import math` and replaced `_sum`'s running-total loop with `return math.fsum(values)`, so the sum is correctly rounded (no accumulated rounding error that propagates into `mean`). I also added a regression test in `tests/test_metrics.py` asserting `_sum([1e16, 1.0, 1.0]) == 1e16 + 2.0`, which a naive running total gets wrong but `math.fsum` gets right.

   ```diff
   --- a/src/evomesh/metrics.py
   +++ b/src/evomesh/metrics.py
   @@ -6,8 +6,5 @@
    
    def _sum(values: Iterable[float]) -> float:
   -    total = 0.0
   -    for value in values:
   -        total += value
   -    return total
   +    return math.fsum(values)
   ```

3. **Change to `tests/test_metrics.py`** — In `src/evomesh/metrics.py` I added `import math` and replaced `_sum`'s running-total loop with `return math.fsum(values)`, so the sum is correctly rounded (no accumulated rounding error that propagates into `mean`). I also added a regression test in `tests/test_metrics.py` asserting `_sum([1e16, 1.0, 1.0]) == 1e16 + 2.0`, which a naive running total gets wrong but `math.fsum` gets right.

   ```diff
   --- a/tests/test_metrics.py
   +++ b/tests/test_metrics.py
   @@ -9,4 +9,8 @@
    
    
   +def test_sum_is_correctly_rounded_for_mixed_magnitudes() -> None:
   +    assert _sum([1e16, 1.0, 1.0]) == 1e16 + 2.0
   +
   +
    def test_mean_returns_the_arithmetic_average() -> None:
        assert mean([1.0, 2.0, 3.0]) == 2.0
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
