# Generation 1397

- **Opened:** 2026-09-24 14:01 UTC
- **Parent generation:** 1396
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `humanize_bytes` is an alias for `humanize_size`, which renders byte counts with binary IEC units at one decimal place once past "B"; 1024 bytes is exactly 1.0 KiB, so the one obvious passing check is that `humanize_bytes(1024) == "1.0 KiB"`.

*Standing goal: Write ONE small, mechanical test for `humanize_bytes()` in `src/evomesh/humanize.py`. It is exported and load-bearing (used by 2 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `humanize_bytes` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: Human-friendly formatting helpers for EvoMesh.*

### What it changed, and the reason it gave

1. **Change to `tests/test_humanize.py`** — `humanize_bytes` is an alias for `humanize_size`, which renders byte counts with binary IEC units at one decimal place once past "B"; 1024 bytes is exactly 1.0 KiB, so the one obvious passing check is that `humanize_bytes(1024) == "1.0 KiB"`.

   ```diff
   --- a/tests/test_humanize.py
   +++ b/tests/test_humanize.py
   @@ -1,5 +1,5 @@
    from datetime import UTC, datetime, timedelta
    
   -from evomesh.humanize import humanize_timestamp
   +from evomesh.humanize import humanize_bytes, humanize_timestamp
   ```

2. **Change to `tests/test_humanize.py`** — `humanize_bytes` is an alias for `humanize_size`, which renders byte counts with binary IEC units at one decimal place once past "B"; 1024 bytes is exactly 1.0 KiB, so the one obvious passing check is that `humanize_bytes(1024) == "1.0 KiB"`.

   ```diff
   --- a/tests/test_humanize.py
   +++ b/tests/test_humanize.py
   @@ -9,2 +9,8 @@
        result = humanize_timestamp(one_day_ago.timestamp())
        assert result.endswith(" days ago")
   +
   +
   +def test_humanize_bytes_1024_renders_as_one_kib():
   +    # One KiB is the smallest non-byte value, rendered with one decimal place.
   +    result = humanize_bytes(1024)
   +    assert result == "1.0 KiB"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
