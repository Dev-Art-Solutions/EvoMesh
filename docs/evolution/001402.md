# Generation 1402

- **Opened:** 2026-09-24 14:17 UTC
- **Parent generation:** 1401
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The test calls `plan_needle(Improvement("Wire up `known_dead", ...))` and asserts it returns exactly `"Plan this improvement to EvoMesh: Wire up `known_dead`", matching the function's f-string body rather than guessing from its name.

*Standing goal: Write ONE small, mechanical test for `plan_needle()` in `src/evomesh/codebase.py`. It is exported and load-bearing (used by 1 other module), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `plan_needle` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: What the package contains and how its modules depend on each other.*

### What it changed, and the reason it gave

1. **Change to `tests/test_codebase.py`** — The test calls `plan_needle(Improvement("Wire up `known_dead", ...))` and asserts it returns exactly `"Plan this improvement to EvoMesh: Wire up `known_dead`", matching the function's f-string body rather than guessing from its name.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -1,4 +1,5 @@
    from evomesh.codebase import (
        PACKAGE,
   +    Improvement,
        fabricated_references,
        known_dead,
   ```

2. **Change to `tests/test_codebase.py`** — The test calls `plan_needle(Improvement("Wire up `known_dead", ...))` and asserts it returns exactly `"Plan this improvement to EvoMesh: Wire up `known_dead`", matching the function's f-string body rather than guessing from its name.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -42,2 +42,8 @@
        (tmp_path / "orphan.py").write_text("# left behind by accident\n")
        assert stray_root_files(tmp_path) == ["orphan.py"]
   +
   +
   +def test_plan_needle_prefixed_with_the_objective_prefix():
   +    assert plan_needle(Improvement("Wire up `known_dead", "In codebase.")) == (
   +        "Plan this improvement to EvoMesh: Wire up `known_dead`"
   +    )
   ```

3. **Change to `tests/test_codebase.py`** — The test calls `plan_needle(Improvement("Wire up `known_dead", ...))` and asserts it returns exactly `"Plan this improvement to EvoMesh: Wire up `known_dead`", matching the function's f-string body rather than guessing from its name.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -5,4 +5,5 @@
        known_dead,
        package_root,
   +    plan_needle,
        stray_root_files,
        survey,
   ```

4. **Repair to `tests/test_codebase.py`** — The test asserted that `plan_needle` would auto-close the stray leading backtick in the title, but `plan_needle` (`src/evomesh/codebase.py:1280`) does no backtick balancing — it only prepends the objective prefix verbatim. Since the code under test cannot be changed, the test's expectation was wrong; I corrected the expected string to the actual output `"Plan this improvement to EvoMesh: Wire up `known_dead"` while keeping the test's real purpose (verifying the prefix) intact.

   ```diff
   --- a/tests/test_codebase.py
   +++ b/tests/test_codebase.py
   @@ -47,4 +47,4 @@
    def test_plan_needle_prefixed_with_the_objective_prefix():
        assert plan_needle(Improvement("Wire up `known_dead", "In codebase.")) == (
   -        "Plan this improvement to EvoMesh: Wire up `known_dead`"
   +        "Plan this improvement to EvoMesh: Wire up `known_dead"
        )
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |

It repaired itself **1 time** before reaching that verdict.
