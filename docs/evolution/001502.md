# Generation 1502

- **Opened:** 2026-09-25 00:24 UTC
- **Parent generation:** 1495
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The four divisors are each one unit too small, so any duration of at least a week (an "ago" age older than a week, or a harness elapsed time past a week) is mis-bucketed and mislabeled — e.g. 1,209,600 s prints as `"14w"` — yet the branch is untested and `humanize_duration`/`humanize_timestamp` are used for real elapsed-time and timestamp output.

*Standing goal: Refill the improvement backlog from src/evomesh/humanize.py: docs/evolution/improvements.md has nothing left to hand out. Find ONE real problem or missing capability in src/evomesh/humanize.py and write it down as one new item with its steps -- do not fix it.
What the module is for: Human-friendly formatting helpers for EvoMesh.
Otherwise look for: a function that does the wrong thing on an input it really gets, error handling that swallows the cause, a fixed number that should come from settings, a loop with no backoff, work repeated every cycle that could be cached.
Already done -- do not propose these again:
  - Back off exponentially when Telegram polling keeps failing, and log what actually failed
  - Let a restarted mesh wait briefly for the previous process to release its lock
  - Stop uv's VIRTUAL_ENV warning from polluting every validation and repair output
  - Show the Evolver's recent success rate in `/evolution status`
  - Actually pass the VIRTUAL_ENV-free environment to the candidate's uv commands
  - Kill the whole process group when run_command's timeout fires
  - Make the watcher's timeout actually kill the command, not just stop awaiting it
  - Make cron treat weekday `7` as Sunday*

### What it changed, and the reason it gave

1. **Change to `docs/evolution/improvements.md`** — The four divisors are each one unit too small, so any duration of at least a week (an "ago" age older than a week, or a harness elapsed time past a week) is mis-bucketed and mislabeled — e.g. 1,209,600 s prints as `"14w"` — yet the branch is untested and `humanize_duration`/`humanize_timestamp` are used for real elapsed-time and timestamp output.

   ```diff
   +- [ ] Render week-plus durations in `humanize_duration` using the true second-counts per unit
   +    `humanize_duration` formats any duration of a week or more in a multi-part string, but its four `divmod` divisors descend one unit level too far: the weeks bucket divides by `days*hours*minutes` (86,400 s = one day), the days bucket by `days*hours` (1,440 s), the hours bucket by `hours` (60 s = one minute), and the minutes bucket then divides by `minutes` (60) a value that is already `< 60`, so minutes is always `0`. A timestamp two weeks ago (`delta` = 1,209,600 s) therefore renders as `"14w ago"` instead of `"2w ago"`, and the branch is only reached for the `humanize_timestamp` "ago" text and for the `harness_session` elapsed / mean-elapsed values once they cross a week. No test covers it (`tests/test_humanize.py` only exercises sub-second durations and sub-1024 byte sizes).
   +    >         weeks, remaining = divmod(remaining, _DAYS * _HOURS * _MINUTES)
   +    1. [ ] src/evomesh/humanize.py `humanize_duration` -- replace the four `divmod` divisors with the correct number of seconds per unit (weeks → `_DAYS * _HOURS * _MINUTES * _WEEKS`, days → `_DAYS * _HOURS * _MINUTES`, hours → `_HO
   ...
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
