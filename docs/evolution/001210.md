# Generation 1210

- **Opened:** 2026-09-21 13:48 UTC
- **Parent generation:** 1174
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** The model changed `src/evomesh/humanize.py` but gave no rationale for it -- see the diff below for what actually moved.

*Standing goal: Improve EvoMesh by one validated candidate generation at a time.*

### What it changed, and the reason it gave

1. **Change to `src/evomesh/humanize.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/humanize.py
   +++ b/src/evomesh/humanize.py
   @@ -10,4 +10,5 @@
    _DAYS = 24
    _WEEKS = 7
   +_MAX_FLOAT = 1.7976931348623157e308
   ```

2. **Change to `src/evomesh/humanize.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/humanize.py
   +++ b/src/evomesh/humanize.py
   @@ -3,4 +3,5 @@
    from __future__ import annotations
    
   +import math
    from datetime import datetime
    
   @@ -10,5 +11,23 @@
    _DAYS = 24
    _WEEKS = 7
   -_MAX_FLOAT = 1.7976931348623157e308
   +
   +
   +def _safe_float(value) -> float:
   +    """Coerce ``value`` to a finite float that round-trips safely.
   +
   +    Raises :class:`ValueError` for ``None``/non-numeric input, NaN,
   +    infinities, and out-of-range values (e.g. an ``int`` larger than the
   +    largest representable ``float``) so they never render as ``"inf"`` or
   +    raise an uncaught ``OverflowError`` mid-format.
   +    """
   +    if not isinstance(value, (int, float)):
   +        raise ValueError(f"expected a number, got {type(value).__name__}")
   +    try:
   +        number = float(value)
   +    except (OverflowError, ValueError) as exc:
   +        raise ValueError(f"{value!r} is too large to render") from exc
   +    if not math.isfinite(number):
   +        raise ValueError(f"{value!r} is not a finite number")
   +    return number
   ```

3. **Change to `src/evomesh/humanize.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/humanize.py
   +++ b/src/evomesh/humanize.py
   @@ -36,5 +36,5 @@
        if num_bytes < 0:
            raise ValueError("num_bytes must be non-negative")
   -    value = float(num_bytes)
   +    value = _safe_float(num_bytes)
        unit = _UNITS[0]
        while value >= 1024 and unit != _UNITS[-1]:
   ```

4. **Change to `src/evomesh/humanize.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/humanize.py
   +++ b/src/evomesh/humanize.py
   @@ -50,5 +50,5 @@
        if seconds is None:
            raise TypeError("seconds must be a number, not None")
   -    seconds = max(0, float(seconds))
   +    seconds = max(0, _safe_float(seconds))
        if seconds < 1:
            return f"{seconds * 1000:.0f} ms"
   ```

5. **Change to `src/evomesh/humanize.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/humanize.py
   +++ b/src/evomesh/humanize.py
   @@ -11,23 +11,4 @@
    _DAYS = 24
    _WEEKS = 7
   -
   -
   -def _safe_float(value) -> float:
   -    """Coerce ``value`` to a finite float that round-trips safely.
   -
   -    Raises :class:`ValueError` for ``None``/non-numeric input, NaN,
   -    infinities, and out-of-range values (e.g. an ``int`` larger than the
   -    largest representable ``float``) so they never render as ``"inf"`` or
   -    raise an uncaught ``OverflowError`` mid-format.
   -    """
   -    if not isinstance(value, (int, float)):
   -        raise ValueError(f"expected a number, got {type(value).__name__}")
   -    try:
   -        number = float(value)
   -    except (OverflowError, ValueError) as exc:
   -        raise ValueError(f"{value!r} is too large to render") from exc
   -    if not math.isfinite(number):
   -        raise ValueError(f"{value!r} is not a finite number")
   -    return number
   ```

6. **Change to `src/evomesh/humanize.py`** — (the model gave no rationale)

   ```diff
   --- a/src/evomesh/humanize.py
   +++ b/src/evomesh/humanize.py
   @@ -11,4 +11,23 @@
    _DAYS = 24
    _WEEKS = 7
   +
   +
   +def _safe_float(value) -> float:
   +    """Coerce ``value`` to a finite float that round-trips safely.
   +
   +    Raises :class:`ValueError` for ``None`` / non-numeric input, NaN,
   +    infinities, and out-of-range values (e.g. an ``int`` larger than the
   +    largest representable ``float``) so they never render as ``"inf"`` /
   +    ``"nan"`` and never raise an uncaught ``OverflowError`` mid-format.
   +    """
   +    if not isinstance(value, (int, float)):
   +        raise ValueError(f"expected a number, got {type(value).__name__}")
   +    try:
   +        number = float(value)
   +    except (OverflowError, ValueError) as exc:
   +        raise ValueError(f"{value!r} is too large to render") from exc
   +    if not math.isfinite(number):
   +        raise ValueError(f"{value!r} is not a finite number")
   +    return number
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
