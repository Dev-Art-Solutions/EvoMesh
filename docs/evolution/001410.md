# Generation 1410

- **Opened:** 2026-09-24 15:06 UTC
- **Parent generation:** 1409
- **Author:** Mesh Evo Agent <mesh-evo-agent@evomesh.local>

## Why this change

**What it set out to do.** `call_key` is the `f"{call.name}:{json.dumps(call.arguments, sort_keys=True, default=str)}"` string that uniquely identifies a tool invocation. A single `ToolCall` (already imported) is the simplest real input, and the assertion checks the one obvious thing the code does — name + colon + deterministic JSON of the arguments — without asserting anything beyond the contract shown in the source.

*Standing goal: Write ONE small, mechanical test for `call_key()` in `src/evomesh/harness.py`. It is exported and load-bearing (used by 3 other modules), but its name does not appear anywhere under tests/, so it has no direct test coverage right now.
Call `call_key` with the simplest realistic arguments and assert the one obvious thing about its result -- one real, passing check is a complete answer, not every branch.
The module's own docstring says what it is for: A model that can look at the project before it answers.*

### What it changed, and the reason it gave

1. **Change to `tests/test_harness.py`** — `call_key` is the `f"{call.name}:{json.dumps(call.arguments, sort_keys=True, default=str)}"` string that uniquely identifies a tool invocation. A single `ToolCall` (already imported) is the simplest real input, and the assertion checks the one obvious thing the code does — name + colon + deterministic JSON of the arguments — without asserting anything beyond the contract shown in the source.

   ```diff
   --- a/tests/test_harness.py
   +++ b/tests/test_harness.py
   @@ -25,4 +25,5 @@
        HarnessRunner,
        build_runner,
   +    call_key,
        compact,
        message_size,
   ```

2. **Change to `tests/test_harness.py`** — `call_key` is the `f"{call.name}:{json.dumps(call.arguments, sort_keys=True, default=str)}"` string that uniquely identifies a tool invocation. A single `ToolCall` (already imported) is the simplest real input, and the assertion checks the one obvious thing the code does — name + colon + deterministic JSON of the arguments — without asserting anything beyond the contract shown in the source.

   ```diff
   --- a/tests/test_harness.py
   +++ b/tests/test_harness.py
   @@ -3120,2 +3120,10 @@
    
        assert runner.context.root == project
   +
   +
   +def test_call_key_is_the_name_joined_with_its_json_arguments() -> None:
   +    call = ToolCall(name="read", arguments={"path": "notes/todo.txt"})
   +
   +    result = call_key(call)
   +
   +    assert result == "read:{\"path\": \"notes/todo.txt\"}"
   ```


## How it was checked

The suite **passed**:

| Command | Exit |
| --- | --- |
| `uv sync` | 0 |
| `uv run ruff check .` | 0 |
| `uv run pyright` | 0 |
| `uv run pytest --basetemp .pytest-tmp` | 0 |
| `uv run python -m evomesh.smoke` | 0 |
